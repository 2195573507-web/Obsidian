---
title: "用户画像"
tags: ['agent', 'config']
updated: 2026-06-24
---

# 用户画像

## 基本信息

- 时区：北京时间（UTC+8）
- 语言偏好：中文
- 沟通渠道：QQ（主要）、Web Dashboard

## 工作风格

- **直接行动派**：说"继续/完成/修复"时，希望助手连续推进到可验证完成
- **少确认多执行**：不需要每步都问"是否继续"，除非涉及破坏性操作
- **优先级灵活**：临时改优先级时立即暂停原计划，不要试图完成旧任务
- **端到端执行**：配置任务要一步到位（如取消反代需同时处理 Nginx + Certbot + reload + 验证）

## 技术栈与环境

- 服务器：公网 IP `38.55.146.137`
- 系统监控：`:9000`（system-monitor）
- Hermes Dashboard：`:9100`
- OpenClaw：`127.0.0.1:18789`
- 共享记忆服务：`:9400`（/opt/shared-agent-memory）
- 所有项目：FastAPI + 原生 HTML/JS/CSS，无现代前端框架
- Docker 根目录：`/var/lib/docker`，数据盘 `/www` 可用于新服务

## 模型路由偏好

- 主模型：中转站 GPT（`myproxy/gpt-5.5`）
- **不使用千问/Qwen**（2026-06-23 用户明确决定）
- 所有请求默认走中转 GPT，复杂/工具/联网/排障/配置/安全/关键决策优先 GPT
- 中转站：`sub.zmjjkkk.fun/v1`（主），`pool.gptstore.club/v1`（备，key 不匹配）
- 排查连接问题时优先测当前中转站 `/models` 和逐模型探活

## 定时任务规范

- 所有时间以北京时间（UTC+8）为准
- cron 表达式需转换为 UTC（BJT - 8h）
- 允许安排任意时段任务（包括 BJT 00:00-07:00），按用户指定执行
- 多任务流水线标注 BJT/UTC 双时区
- 所有任务输出使用中文

## 维护策略

- 北京时间 09:00-23:00 每小时轮流巡检记忆健康
- 只做健康检查、过期处理、导出/索引和候选报告
- 删除/禁用/合并重要记忆需人工确认
- skills 整理、文件整理、Docker/日志清理需先出报告等确认

## 研究与学习偏好

- 多 agent 并行、长篇全面中文报告
- 说"不改服务器代码"则只写报告并给保存路径
- 长时间任务（7h+）用 cron 多轨道比 delegate_task 更稳定
- 做管理后台二开时，新功能应原生集成到现有 UI，手机端不拥挤

## UI/UX 偏好

- 新功能原生集成到现有 UI，不做独立页面
- 手机端不拥挤，按钮操作有明确成功/失败反馈
- Dashboard 优先做只读服务卡片、端口、systemd/docker、Nginx 摘要
