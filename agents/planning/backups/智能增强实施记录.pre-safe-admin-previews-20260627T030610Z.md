---
title: Hermes / OpenClaw 智能增强实施记录
tags: [agents, hermes, openclaw, implementation, roadmap]
created: 2026-06-25
updated: 2026-06-25
---

# Hermes / OpenClaw 智能增强实施记录

关联：[[Hermes-OpenClaw-智能增强路线图]]、[[MOC]]、[[服务器架构]]、[[shared-memory/exports/shared-memory|共享记忆导出]]

## 2026-06-25 阶段 1 启动

目标：按路线图从“Obsidian + shared-memory + 自动召回”开始，逐步完成智能增强。

### 已完成：Obsidian Knowledge Web RAG 只读接口

服务：`obsidian-knowledge-web.service`，端口 `9200`。

新增接口：

- `GET /api/search?q=...`
  - 搜索 path/title/tags/frontmatter/body。
  - 返回 score、snippet、headings、wikilinks。
- `GET /api/context?q=...`
  - 返回适合 agent 注入上下文的 compact blocks 和 markdown。
- `POST /api/reindex`
  - 手动重建内存索引。

安全边界：

- 只读 vault。
- 不删除、不移动、不改写历史笔记。
- 保持原 `/api/status`、`/api/files`、`/api/file`、`/api/graph` 和前端兼容。

备份：

- `/opt/obsidian-knowledge-web/backups/main.py.pre-rag-20260625T154822Z`
- `/opt/obsidian-knowledge-web/backups/index.html.pre-rag-20260625T154822Z`

### 已完成：Agent Recall Gateway 统一召回网关

服务：`agent-recall-gateway.service`，本机端口 `127.0.0.1:19410`。

作用：只读聚合：

- `shared-agent-memory :9400`
- `obsidian-knowledge-web :9200`

接口：

- `GET /health`
- `GET /api/recall?q=...&mode=default|debug|planning|all`
- `GET /api/recall.md?q=...`

安全边界：

- 只读。
- 输出做基础 secret-like redaction。
- 不改变 Hermes/OpenClaw 主回复路由。

### 已完成：Provider Health Monitor 初版

服务：`provider-health-monitor.service`，本机端口 `127.0.0.1:19420`。

接口：

- `GET /health`
- `POST /api/check-now`
- `GET /api/latest`
- `GET /api/history`

探测范围：

- 只探测 `/v1/models`。
- 不做 `/v1/chat/completions` 压测。
- 读取本机既有配置中的 key 仅用于 Authorization，不输出、不入库。

初次结果：

- `hermes-local-proxy`：200，约 119ms，10 models。
- `subus-zmjjkkk`：200，约 123ms，10 models。
- `sub-zmjjkkk`：401 Unauthorized，约 775ms。

### 后续队列

1. 将 `agent-recall-gateway` 接入 Dashboard 检索面板。
2. 为 Provider Health Monitor 增加 QQ 告警策略：连续失败 N 次才提醒，不自动切路由。
3. 增加 Hermes / OpenClaw 自愈巡检服务：先只报告。
4. 再推进任务队列中心、日志索引、QQ 指令菜单等阶段。

## 2026-06-25 阶段 2：ServerHub Next 只读接入

### 已完成：统一控制台服务卡片

`/opt/serverhub-next/services.yml` 新增：

- `recall-gateway`：Agent 统一召回，端口 `19410`，unit `agent-recall-gateway.service`。
- `provider-health`：中转健康监控，端口 `19420`，unit `provider-health-monitor.service`。

### 已完成：ServerHub Next 只读 API 代理

服务：`serverhub-next.service`，端口 `9600`。

新增接口：

- `GET /api/recall?q=...`
  - 代理到 `127.0.0.1:19410/api/recall`。
  - 用于 Dashboard 页面测试统一召回。
- `GET /api/provider-health`
  - 代理到 `127.0.0.1:19420/api/summary`。
  - 展示各中转目标最近状态、成功率、连续失败次数和 report-only 告警。

### 已完成：Provider Health Monitor 告警摘要

`provider-health-monitor.service` 新增：

- `GET /api/summary?window=30&alert_after=3`

策略：

- 连续失败达到阈值只标记 alert。
- 不自动切换模型路由。
- 不压测 chat/completions。
- 仅 `/v1/models` 探测。

当前摘要：

- `hermes-local-proxy`：正常。
- `subus-zmjjkkk`：正常。
- `sub-zmjjkkk`：连续 8 次 401，alert=true。

### 验证

- `python3 -m py_compile /opt/serverhub-next/backend.py` passed。
- `node --check` ServerHub inline JS passed。
- `serverhub-next.service` active。
- `/api/state` 200，10 个服务在线。
- `/api/provider-health` 200。
- `/api/recall?q=Hermes OpenClaw 自动召回` 200，返回 shared-memory 5 条 + Obsidian 5 条。

### 备份

- `/opt/serverhub-next/backups/backend.py.pre-recall-health-20260625T160424Z`
- `/opt/serverhub-next/backups/index.html.pre-recall-health-20260625T160424Z`
- `/opt/serverhub-next/backups/services.yml.pre-recall-health-20260625T160424Z`
- `/opt/serverhub-next/backups/backend.py.pre-provider-summary-*`
- `/opt/serverhub-next/backups/index.html.pre-provider-summary-*`
- `/opt/provider-health-monitor/backups/main.py.pre-summary-*`

## 2026-06-25 阶段 2b：重构新控制台替代 ServerHub

用户反馈：`serverhub我不想要了，能否重构一个`。

处理策略：

- 不删除、不停止原 `serverhub-next.service`，避免破坏现有入口。
- 新建独立控制台 `hermes-control-center.service`，端口使用五位数 `19600`。
- 后续验证满意后，再决定是否切换域名/入口。

### 新服务

- 路径：`/opt/hermes-control-center`
- Unit：`hermes-control-center.service`
- 端口：`19600`
- 首页：`http://127.0.0.1:19600/`

### 新接口

- `GET /api/health`
- `GET /api/overview`
- `GET /api/provider-summary`
- `GET /api/recall?q=...`
- `GET /api/logs/{unit}`

### 新前端组织

- 总览：关键服务、CPU/内存/磁盘/在线数。
- 服务：服务全图和日志入口。
- 知识召回：调用 `19410` 统一召回网关。
- 中转健康：调用 `19420` provider summary，report-only。
- 日志：受限 unit 白名单，只读 journalctl。

### 验证

- `python3 -m py_compile /opt/hermes-control-center/backend.py` passed。
- inline JS `node --check` passed。
- `hermes-control-center.service` active。
- `0.0.0.0:19600` listening。
- `/api/health` 200。
- `/api/overview` 200。
- `/api/provider-summary` 200。
- `/api/recall?q=OpenClaw 自动召回` 200，返回 shared-memory 5 条 + Obsidian 5 条。
- 首页 HTML 200，包含 `Hermes Control Center` 与 `新的统一控制台`。

备注：浏览器自动打开因当前 browser tool policy 阻止，已用 curl + JS/HTML 检查替代。

## 2026-06-25 阶段 2c：全 Web 重命名与 UI 重构

用户要求：服务器已有 Web 除 upstream/sub2api 外全部重新命名，不再以 Hermes/OpenClaw 命名；重新配置端口；删除旧前端/UI 并使用不同风格重构。

### 安全边界

未改动：

- `upstream.zmjjkkk.fun` → `127.0.0.1:8420`
- `subus.zmjjkkk.fun` / `apius.zmjjkkk.fun` → sub2api / upstream API 相关配置
- 本地中转 `127.0.0.1:18888`
- Docker 暴露 `8080/8420`
- OpenClaw gateway 运行时服务本身

### 新命名与端口

| 新服务 | 端口 | 说明 |
|---|---:|---|
| `atlas-control.service` | 19600 | 新统一控制台 |
| `atlas-observe.service` | 19000 | 系统观测/原监控 |
| `atlas-agents.service` | 19100 | Agent 状态面板 |
| `atlas-notes.service` | 19200 | Vault/RAG 知识库 |
| `atlas-files.service` | 19300 | 文件浏览 |
| `atlas-memory.service` | 19400 | shared-memory API |
| `atlas-recall.service` | 19410 | 统一召回 API |
| `atlas-provider-watch.service` | 19420 | 中转健康观察 |
| `atlas-daily.service` | 19500 | 每日总结 |
| `atlas-collab.service` | 19700 | 协作板 |

### 停用旧 Web 服务

已 disable + inactive：

- `hermes-control-center.service`
- `hermes-system-monitor.service`
- `hermes-dashboard-web.service`
- `obsidian-knowledge-web.service`
- `server-file-web.service`
- `shared-agent-memory.service`
- `agent-recall-gateway.service`
- `provider-health-monitor.service`
- `hermes-daily-summary-web.service`
- `agent-collab-web.service`
- `serverhub-next.service`
- `server-home.service`

### UI 重构

旧前端/UI 已归档备份，新前端统一改为 Atlas 风格：

- 明亮纸面背景
- 粗线框
- 硬阴影
- 高对比色块
- 不再使用旧 dashboard 暗色玻璃风格

已验证首页指纹：

- `19000` → `Atlas Observe`
- `19100` → `Atlas Agents`
- `19200` → `Atlas Notes`
- `19300` → `Atlas Files`
- `19500` → `Atlas Daily`
- `19600` → `Atlas Control`
- `19700` → `Atlas Collab`

### Nginx 入口迁移

非中转 Web 入口更新：

- `monitor.zmjjkkk.fun` → `127.0.0.1:19000`
- `agent.zmjjkkk.fun` → `127.0.0.1:19100`
- `notes.zmjjkkk.fun` → `127.0.0.1:19200`
- `server.zmjjkkk.fun` → `127.0.0.1:19600`

中转相关入口保持不变：

- `upstream.zmjjkkk.fun` → `127.0.0.1:8420`
- `subus.zmjjkkk.fun` → `127.0.0.1:8080`
- `apius.zmjjkkk.fun` 保持原配置

### 验证

全量验证日志：

- `/root/.openclaw/workspace/rebrand-web-final-20260625T164303Z.log`

结果：

- 10 个 `atlas-*` 服务全部 active。
- 旧 Web 服务全部 inactive/disabled。
- 旧端口 `7000/8000/9000/9100/9200/9300/9400/9500/9600` 已释放。
- 新端口 `19000/19100/19200/19300/19400/19410/19420/19500/19600/19700` 正常监听。
- 10 个本地 API 全部 HTTP 200。
- `nginx -t` passed。
- Nginx 本地 Host 复测：`monitor/agent/notes/server/upstream/subus/apius` 全部 HTTP 200。
- `atlas-recall` 召回测试 HTTP 200，返回 shared-memory 5 条 + Obsidian 5 条，errors=0。

### 备份

主要备份位置：

- `/root/.openclaw/workspace/backups/web-rebrand-20260625T162445Z`
- `/etc/nginx/sites-available/zmjjkkk.fun.pre-atlas-*`
- 各 `/opt/atlas-*/backups/` 中保存旧 UI 文件。

## 2026-06-25 阶段 2d：Atlas 前端功能补全

用户要求继续完成前端功能，并指出应学习 upstream UI 风格。

### 完成内容

- Atlas UI 继续保持 upstream 风格方向：顶部栏、居中内容、shadcn-like card/table/form/badge。
- `Atlas Observe` 补回历史曲线：轻量 SVG 折线图，使用 `/api/system/metrics/history?range=1h`。
- `Atlas Agents` 补回 Provider 管理卡片：baseUrl/models/enabled/API key 空值保留；保留 agent 重启/日志入口。
- `Atlas Notes` 补回 graph、context、Markdown 渲染和文件打开。
- `Atlas Files` 新增后端 `/api/preview` 和 `/api/raw`，前端补回路径摘要、quick paths、文本/图片预览。
- `Atlas Daily` 补回 Markdown 渲染和分类/文档列表阅读。
- `Atlas Control` 保留服务地图、中转、召回、日志入口。

### 安全修复

`Atlas Agents` 原 `/api/config/providers` 和 `/api/config` 会返回真实 API key。已修复：

- `/api/config/providers` 中 `apiKey` 永远为空，仅返回 `apiKeyMasked`。
- `/api/config` 的 `raw` 字段递归脱敏 key/token/secret/password/cookie/authorization。
- Provider 保存时 API key 留空不会覆盖原 key。

### 验证

- 7 个前端 inline JS `node --check` passed。
- `atlas-agents.service` 和 `atlas-files.service` 重启后 active。
- Provider/config 脱敏验证：无真实 `sk-*` key 泄露，`apiKey` 为空。
- 公网入口 `monitor/agent/notes/server` 均 HTTP 200。
- 功能接口验证：Observe history、Agents providers、Notes graph/context、Files preview、Daily summaries、Collab tasks 均 200。

## 2026-06-25 阶段 2e：Atlas 前端功能第二轮补齐

用户继续要求完成前端功能。本轮重点补旧前端的可操作细节。

### 完成内容

- `Atlas Observe`
  - 增加 `/api/system/metrics/detailed` 接入。
  - 增加 API / HTTP / Docker / DB / TCP 等诊断曲线卡片：`api_local_latency`、`api_apius_latency`、`http_5xx`、`docker_total_cpu`、`pg_active_connections`、`tcp_retrans_ratio`。
  - 增加 CPU per-core 卡片。
- `Atlas Agents`
  - Provider 管理补齐新增、保存、测试并拉取模型、设为活跃、启停、删除。
  - 增加最近 sessions 表。
  - 继续保持密钥脱敏和空 key 不覆盖。
- `Atlas Files`
  - `/api/raw?download=1` 增加 `Content-Disposition: attachment` 下载头。
  - 前端补充面包屑、文件类型图标、预览/下载/原始打开动作。
- `Atlas Daily`
  - 增加分类筛选、过滤计数和 Markdown 阅读布局。

### 验证

- 7 个 Atlas 前端 inline JS `node --check` 通过。
- `atlas-files`、`atlas-agents`、`atlas-observe`、`atlas-daily` 重启后 active；7 个 Atlas 服务均 active。
- 功能接口验证 200：Observe detailed/history、Agents providers、Files raw download、Daily summaries。
- Files raw download header 验证：`Content-Disposition: attachment`。
- 页面指纹验证：Observe `API / HTTP / Docker`、`CPU Cores`；Agents `新增 Provider`、`测试并拉取模型`；Files `Path Summary`/`下载`；Daily `分类筛选`/`Filtered`。
- `nginx -t` successful。

## 2026-06-26 阶段 2f：Atlas 前端功能第三轮增强

用户继续要求完成。基于第二轮已完成的功能，本轮补齐交互细节。

### 完成内容

- `Atlas Control`
  - 服务卡新增“详情”入口。
  - 新增 Service Detail 视图：systemd、port、HTTP health、原因列表、打开页面、查看日志。
  - 日志视图新增前端过滤输入框。
- `Atlas Agents`
  - Provider 测试结果中的模型可形成勾选列表。
  - 新增“保存勾选模型”，调用 `/api/config/providers/{name}/models`。
  - 保留文本框直接编辑 models 的路径。
- `Atlas Notes`
  - 图谱按 category 着色。
  - 增加 category 筛选按钮。
  - 点击节点后高亮自身和邻接边/节点。
  - 增加 Neighbors 列表，点击邻接项打开文件。
- `Atlas Files`
  - 增加按名称/大小/类型排序。
  - 增加隐藏/显示 dotfiles 开关。
  - 预览区保留下载、原始打开、截断提示。

### 验证

- 7 个 Atlas 前端 inline JS `node --check` 通过。
- 7 个 Atlas 服务均 active。
- 页面指纹验证：Control `详情/过滤日志/服务详情`；Agents `模型勾选/保存勾选模型`；Notes `分类图谱/邻接`；Files `按大小/隐藏 dotfiles/Path Summary`。
- 接口验证 200：Control overview/logs、Notes graph、Files list、Agents providers。
- 公网 host 验证 200：monitor、agent、notes、server。
- `nginx -t` successful。


## 2026-06-26 阶段 2g：Atlas UI 重构任务归档

用户明确要求“UI 重构的任务先归档”。已将 Atlas UI 重构阶段写入路线图归档区，并停止把 UI 细节修补作为当前主线。

归档结论：

- Atlas Web/UI 重构阶段已完成到可用状态。
- 后续只做必要维护，不继续作为主线推进。
- 下一主线按路线图回到“马上值得做”：Obsidian + shared-memory + Hermes/OpenClaw 自动召回、自愈巡检、任务队列中心。

备份：路线图和实施记录已在同目录生成 `.bak-ui-archive-*`。

## 2026-06-26 阶段 3a：自动召回接口第一步

按路线图从已归档的 UI 重构切回“Obsidian + shared-memory + Hermes/OpenClaw 自动召回”主线。

### 完成内容

- `atlas-recall.service` 从单纯 `/api/recall` 聚合接口增强为 agent-friendly recall gateway。
- 新增兼容健康接口：`/api/health`（保留 `/health`）。
- 新增自动召回上下文接口：
  - `GET /api/context?q=...`
  - `GET /api/context.md?q=...`
- `/api/context` 默认返回精简上下文 bundle：
  - `context`：脱敏后的 Markdown 上下文
  - `sources.memories`：命中的 shared-memory id
  - `sources.notes`：命中的 Obsidian path
  - `counts/errors`：召回数量和错误
- 继续保留 `/api/recall` 和 `/api/recall.md`。

### 验证

- `python3 -m py_compile /opt/atlas-recall/main.py` 通过。
- `atlas-recall.service` 重启后 active。
- `/health`、`/api/health`、`/api/context`、`/api/context.md` 均 HTTP 200。

### 下一步

- 将自动召回能力接入 Dashboard/Control 或命令入口。
- 为 Hermes/OpenClaw 常见问题建立召回 query 模板。
- 继续推进路线图中的自愈巡检和任务队列。

补充验证：`/api/context` 和 `/api/context.md` 对中文 query 需要客户端 URL encode；使用 `curl -G --data-urlencode` 复测通过，均 HTTP 200。

修复：`/api/context.md` 初版内部调用未显式传 `max_chars`，FastAPI `Query` 默认对象进入比较导致 500。已修复并复测 `/api/context.md` HTTP 200。

## 2026-06-26 阶段 3b：召回模板与只读自愈巡检

继续按路线图推进，不再继续 UI 重构主线。

### 自动召回模板

`atlas-recall.service` 新增常用模板接口：

- `GET /api/templates`
- `GET /api/template/{name}`
- `GET /api/template/{name}.md`

当前模板：

- `openclaw-no-reply`：OpenClaw 不回消息 / QQBot 异常。
- `provider-health`：中转站健康 / 模型路由异常。
- `server-architecture`：服务器架构与 Atlas 服务。
- `memory-policy`：记忆规则 / 用户偏好 / 决策。
- `atlas-ui-archive`：Atlas UI 重构归档。

修复：初版 `.md` 路由被 `/api/template/{name}` 动态路由抢先匹配，已调整路由顺序，`/api/template/provider-health.md` 复测 200。

### 只读自愈巡检

`atlas-control.service` 新增：

- `GET /api/self-check`

特点：

- report-only，不重启、不修改配置、不自动修复。
- 检查 10 个 Atlas 服务 systemd/端口/HTTP health。
- 检查 Provider Watch alert。
- 检查 Atlas Recall health。
- 执行 `nginx -t` 只读验证。
- 检查内存和根分区使用率。
- 输出 findings、severity、evidence、suggestion。

### 验证

- `atlas-recall.service`、`atlas-control.service` 重启后 active。
- `/api/templates` 200。
- `/api/template/provider-health` 200。
- `/api/template/provider-health.md` 200。
- `/api/self-check` 200。
- `nginx -t` successful。

当前巡检结果：10 个 Atlas 服务均 online；发现 1 个 warning：`sub-zmjjk`/`sub-zmjjkkk` provider 持续 401 Unauthorized。该巡检只报告，不自动切换路由。

## 2026-06-26 阶段 3c：Atlas Collab 任务队列 MVP

用户允许使用 Codex 写代码。尝试调用 Codex CLI，但当前 Codex CLI `exec` 参数兼容性/沙箱运行异常导致未产出可用修改；为避免阻塞，直接按同一规格在 `Atlas Collab` 中实现任务队列 MVP。修改范围限定在 `/opt/atlas-collab`。

### 完成内容

`atlas-collab.service` 新增本地后台任务队列中心，使用既有 SQLite `collab.db` 持久化。

新增数据表：

- `queue_jobs`

字段：

- `id`
- `title`
- `kind`
- `status`: `queued|running|done|failed|cancelled`
- `priority`
- `payload_json`
- `result_json`
- `logs_json`
- `attempts`
- `created_at`
- `updated_at`
- `started_at`
- `finished_at`

新增后端接口：

- `GET /api/queue/status`
- `GET /api/queue/jobs?limit=&status=`
- `POST /api/queue/jobs`
- `GET /api/queue/jobs/{id}`
- `POST /api/queue/jobs/{id}/cancel`
- `POST /api/queue/jobs/{id}/retry`
- `POST /api/queue/jobs/{id}/append-log`

后台 worker：

- 进程内 daemon thread。
- 只处理安全内置 job kind。
- 不执行 shell，不杀进程，不发外部消息。

支持 job kind：

- `note`：立即完成，将 payload 存为 result。
- `recall`：调用 `http://127.0.0.1:19410/api/context?q=...` 并保存结果。
- `self-check`：调用 `http://127.0.0.1:19600/api/self-check` 并保存结果。
- 未知 kind：安全失败并记录错误。

前端：

- `Atlas Collab` 增加“任务队列”导航。
- 队列状态卡：queued/running/done/failed。
- 创建 job 表单：title/kind/priority/payload JSON。
- job 列表：状态、标题、类型、优先级、尝试次数、更新时间、操作。
- job 详情：logs/result、取消、重试、追加日志。

### 验证

- `python3 -m py_compile /opt/atlas-collab/main.py` 通过。
- 前端 inline JS `node --check` 通过。
- `atlas-collab.service` 重启后 active。
- `GET /api/queue/status` 200，worker 为 running。
- `POST /api/queue/jobs` 创建 `note` job 201，worker 自动处理为 done。
- `POST /api/queue/jobs` 创建 `self-check` job 201，worker 自动处理为 done，结果包含 self-check summary/checks。
- `POST /api/queue/jobs` 创建 `recall` job 201，worker 自动处理为 done，结果包含 recall context/sources。
- `append-log` 验证成功。
- `retry` 验证成功，job 重新 queued 后被 worker 再次处理为 done。
- 页面指纹验证：`任务队列`、`Atlas Queue`、`Create Job`、`Selected Job`。
- `nginx -t` successful。

### 说明

该 MVP 仅做本地任务队列和安全内置任务，不做自动外部通知、不执行任意命令、不自动修复系统。

## 2026-06-26 阶段 3d：Atlas Queue 结果归档到 Obsidian

继续路线图第 11 项“任务队列 / 后台任务中心”，在已有 Atlas Collab 队列 MVP 上补齐“结果保存到 Obsidian”。用户允许使用 Codex 写代码；尝试调用 Codex CLI，但本机 Codex `exec` 会在沙箱运行阶段 SIGTERM，未产出可靠修改，因此直接接手实现。

### 完成内容

`atlas-collab.service` 新增手动归档接口：

- `POST /api/queue/jobs/{id}/archive`

请求体：

```json
{
  "dir": "optional-relative-subdir",
  "note": "optional archive note"
}
```

行为：

- 默认写入 `/root/obsidian-vault/agents/reports/atlas-queue/`。
- 文件名包含 UTC 时间、job kind、job id、安全 slug。
- Markdown 包含 frontmatter、job metadata、payload、result、logs。
- 写入前做路径约束：只允许归档到 `atlas-queue` 报告根目录或其子目录。
- 拒绝绝对路径/父级穿越逃逸。
- 归档后追加 job log：`archived to ...`。
- 输出做基础 secret-like redaction，避免把 key/token/secret/password/Bearer/sk-* 直写报告。

前端：

- 选中 job 详情增加“归档到 Obsidian”按钮。
- job 表格增加“归档”按钮。
- 归档时可填写备注。
- 成功后 toast 显示保存路径。

### 验证

- `python3 -m py_compile /opt/atlas-collab/main.py` 通过。
- 前端 inline JS `node --check` 通过。
- `atlas-collab.service` 重启后 active。
- 创建 note job 后，`POST /api/queue/jobs/{id}/archive` 返回 200。
- 已生成验证报告：`/root/obsidian-vault/agents/reports/atlas-queue/20260626T015332Z-note-beed8ac13996-Archive-verification.md`。
- 路径穿越验证：`dir="../../../../tmp"` 返回 400，未写出报告根目录。
- 页面指纹验证：`归档到 Obsidian`、`归档备注`、`archiveJob`。
- `nginx -t` successful。

### 安全边界

本阶段仍是手动归档，不做自动外部通知，不自动修复系统，不执行任意 shell。

## 2026-06-26 阶段 3e：自动召回稳定性修复与端到端 Probe

继续路线图“Obsidian + shared-memory + Hermes/OpenClaw 自动召回”主线，对已完成的 Atlas Recall 链路做稳定性补齐。

### 背景

复测 `atlas-recall.service` 模板时发现：

- `shared-memory` 单次 hybrid 召回偶发需要约 7 秒以上。
- `atlas-recall` 原默认上游 timeout 为 6 秒，导致模板接口偶发只返回 Obsidian notes，`errors` 中出现 shared-memory timeout。
- 端口链路经复核已统一为 Atlas 新端口：`19400/19200/19410/19420`；`19600` 是 Atlas Control 自身端口，不属于旧端口残留。

### 完成内容

`atlas-recall.service` 更新：

- 默认 `RECALL_HTTP_TIMEOUT` 从 6 秒提高到 8 秒。
- 新增分源 timeout：
  - `RECALL_MEMORY_TIMEOUT` 默认 12 秒。
  - `RECALL_OBSIDIAN_TIMEOUT` 默认 6 秒。
- shared-memory hybrid 搜索改用 memory 专属 timeout。
- Obsidian context 搜索改用 Obsidian 专属 timeout。
- 健康检查按上游类型使用分源 timeout。
- 新增端到端自检接口：`GET /api/probe?q=...&mode=...`。

`/api/probe` 返回：

- `ok`
- `counts`
- `errors`
- `elapsed_ms`
- `upstreams`
- `timeouts`

用途：Dashboard、QQ 指令、队列和巡检可一键确认自动召回链路是否同时命中 shared-memory 与 Obsidian。

### 验证

- 已备份：`/opt/atlas-recall/backups/main.py.pre-probe-timeout-20260626T093416Z`。
- `python3 -m py_compile /opt/atlas-recall/main.py` 通过。
- `atlas-recall.service` restart 后 active。
- `GET /health` 200。
- `GET /api/probe` 200，约 6.7s，返回 memories=3、notes=3、errors=0。
- `GET /api/template/openclaw-no-reply` 200，返回 memories=6、notes=6、errors=0。
- `GET /api/context?q=OpenClaw 不回消息` 200，返回 memories=4、notes=4、errors=0。
- `GET /api/self-check` 200，10 个 Atlas 服务 online，critical=0。
- Atlas Collab 创建 recall job 后自动完成，结果包含 Shared Memory + Obsidian context，errors=0。

### 当前状态

自动召回基础链路已稳定：

- Atlas Memory：`127.0.0.1:19400`
- Atlas Notes：`127.0.0.1:19200`
- Atlas Recall：`127.0.0.1:19410`
- Atlas Provider Watch：`127.0.0.1:19420`
- Atlas Control 自检：`127.0.0.1:19600/api/self-check`
- Atlas Collab 队列 recall job：可调用 `19410/api/context` 并保存结果。

### 下一步

- 将 `/api/probe` 接入 Atlas Control 自检展示或 QQ 指令预览。
- 补标准化 recall schema 与更完整脱敏策略。
- 继续推进“自动触发召回编排”：让常见巡检/队列/QQ 命令默认先拉取 `/api/context`。

## 2026-06-26 阶段 3f：Recall Schema 标准化、脱敏增强与 QQ 指令 Probe 接入

继续路线图“自动召回 + QQ 指令菜单 + 自愈巡检”主线，在阶段 3e 的稳定性修复基础上补齐调用方契约。

### 完成内容

`atlas-recall.service`：

- `GET /api/recall` 新增 `schema_version: recall.v1`。
- `GET /api/context` 新增 `schema_version: recall.context.v1`。
- `GET /api/probe` 新增 `schema_version: recall.probe.v1`。
- `counts` 统一增加 `items` 字段。
- 新增统一 `items` 列表，兼容保留 `memories` 和 `notes`。
- memory/note item 统一字段：`source/type/id/title/path/kind/subject/content/snippet/tags/score/updated_at`。
- 脱敏增强：覆盖 `api_key/token/secret/password/authorization/cookie`、`Bearer ...`、`sk-*`、URL query 中的 `access_token/refresh_token`。
- 对 `errors`、`markdown/context`、tags/title/subject/snippet 等输出做统一脱敏。

`atlas-control.service`：

- 新增 `GET /api/recall-probe`，代理 `Atlas Recall /api/probe`。
- `GET /api/self-check` 接入 recall probe：
  - `checks.recall_probe_ok`
  - `checks.recall_probe.elapsed_ms`
  - `checks.recall_probe.counts`
  - probe 异常时生成 recall warning。
- 拆分 recall health 与 probe 探测，避免其中一个失败导致两者同时误报。
- 新增只读 QQ 指令 API：
  - `GET /api/qq-commands`
  - `POST /api/qq-command`
- `/服务器巡检` 的 QQ 预览和执行结果显示自动召回 probe 状态。
- `/查记忆 xxx` 的 QQ 执行结果显示 memory / notes 命中数量。
- Control 前端 Recall 面板增加 `Probe` 按钮，调用 `/api/recall-probe`。

### 验证

- 备份：
  - `/opt/atlas-recall/backups/main.py.pre-schema-probe-20260626T093958Z`
  - `/opt/atlas-control/backups/backend.py.pre-recall-probe-20260626T093958Z`
  - `/opt/atlas-control/backups/index.html.pre-recall-probe-20260626T093958Z`
- `python3 -m py_compile /opt/atlas-control/backend.py /opt/atlas-recall/main.py` 通过。
- Atlas Control inline JS `new Function(...)` 检查通过。
- `atlas-recall.service`、`atlas-control.service` 重启后 active。
- `GET /api/recall` 200：`schema_version=recall.v1`，memories=5、notes=5、items=10、errors=0。
- `GET /api/context` 200：`schema_version=recall.context.v1`，memories=4、notes=4、items=8、errors=0。
- `GET /api/probe` 200：`schema_version=recall.probe.v1`，memories=3、notes=3、items=6、errors=0。
- `GET /api/recall-probe` 200，返回 probe counts。
- `GET /api/self-check` 200：10 个 Atlas 服务 online，critical=0，recall health/probe 均 ok；仅保留已知 provider warning `sub-zmjjkkk`。
- `GET /api/qq-commands` 200，返回 `/查中转`、`/服务器巡检`、`/查记忆`、`/今日任务`。
- `POST /api/qq-command` dry-run `/服务器巡检` 200，预览包含 `Atlas Recall /api/probe`。
- `POST /api/qq-command` execute `/服务器巡检` 200，回复预览包含 `召回 probe 正常（memory 3 / notes 3）`。
- `POST /api/qq-command` execute `/查记忆 OpenClaw 不回消息` 200，回复预览包含 `memory 5 / notes 5`。
- Control 页面指纹：`runRecallProbe` 和 `Probe` 按钮存在。
- `nginx -t` successful。

### 当前状态

自动召回链路已有稳定契约和管理入口：

- Agent/队列可用 `/api/context` 获取压缩上下文。
- Dashboard/自检/QQ 可用 `/api/probe` 验证 shared-memory + Obsidian 双源召回。
- QQ 指令菜单已有本地只读 API 原型，尚未绑定到真实 QQBot 指令分发层。

### 下一步

- 将 QQBot 实际消息入口与 `/api/qq-command` 串起来，或在 OpenClaw 回复逻辑中复用同样命令语义。
- 为 provider health 增加连续失败告警策略和通知节流。
- 继续推进自愈巡检的候选修复建议，但保持 report-only，不自动重启/切路由。

## 2026-06-26 阶段 3g：Provider 告警节流与自愈巡检候选建议

继续路线图“Provider health + 自愈巡检 + 安全修复建议”主线，保持 report-only，不自动切换路由、不自动重启服务、不主动发送外部通知。

### 完成内容

`atlas-provider-watch.service`：

- 新增 `ALERT_COOLDOWN_SECONDS`，默认 1800 秒。
- SQLite 新增 `alert_state` 表：
  - `name`
  - `first_seen_at`
  - `last_seen_at`
  - `last_notified_at`
  - `acknowledged_at`
  - `silenced_until`
  - `active`
  - `consecutive_failures`
  - `message`
- `/api/summary` 增加：
  - `alert_policy`
  - `notification_due`
  - 每个 alert 的 `alert_state`
- 新增只读/状态管理 API：
  - `GET /api/alerts`
  - `POST /api/alerts/{name}/ack`
  - `POST /api/alerts/{name}/notify-mark`
  - `POST /api/alerts/{name}/silence?seconds=...`
- 告警策略仍为状态输出：外部通知发送器可消费 `notification_due`，发送后调用 `notify-mark`；Provider Watch 自身不主动发消息。

`atlas-control.service`：

- `/api/self-check` findings 增加结构化 report-only remediation：
  - `scope`
  - `actionability`
  - `remediation.kind=report-only`
  - `remediation.execute=false`
  - `remediation.risk`
  - `remediation.steps`
  - `remediation.command`
- service/provider/recall/nginx/system 各类 finding 均有候选修复建议。
- provider finding 透传 `alert_state`，可看到是否 silenced、是否 notification_due、cooldown 秒数。
- summary 新增 `report_only_remediations` 计数。

### 验证

- 备份：
  - `/opt/atlas-provider-watch/backups/main.py.pre-alert-throttle-20260626T095235Z`
  - `/opt/atlas-control/backups/backend.py.pre-remediation-suggestions-20260626T095235Z`
- `python3 -m py_compile /opt/atlas-provider-watch/main.py /opt/atlas-control/backend.py` 通过。
- `atlas-provider-watch.service` 与 `atlas-control.service` 重启后 active。
- `GET /health` 200。
- `POST /api/check-now` 200。
- `GET /api/summary` 200，返回 `alert_policy.cooldown_seconds=1800` 与 `notification_due`。
- `GET /api/alerts` 200。
- `POST /api/alerts/sub-zmjjkkk/ack` 200。
- `POST /api/alerts/sub-zmjjkkk/notify-mark` 200。
- `POST /api/alerts/sub-zmjjkkk/silence?seconds=600` 200。
- silence 后复查 `/api/summary`：`notification_due=false`、`silenced=true`。
- `GET /api/self-check` 200：10 个 Atlas 服务 online，critical=0，warning=1，`report_only_remediations=1`。
- provider finding 顶层 `actionability=2-safe-check`，remediation 内部 `actionability=2-safe-check`，分级一致。
- `nginx -t` successful。

### 当前状态

Provider 告警已经具备节流/确认/静默状态管理；自愈巡检已经从“发现问题”升级到“给出结构化、只读、需人工确认的候选修复建议”。

### 下一步

- 把 `notification_due` 接入可选通知发送器，发送后自动调用 `notify-mark`，但仍不改变路由。
- 在 Control 前端展示 remediation/actionability。
- 继续推进队列/任务侧自动预检：任务执行前先读 `/api/self-check` 和 `/api/recall-probe`。

## 2026-06-26 阶段 3h：Provider 通知预览与 Control 自检建议 UI

继续路线图“通知节流 + Dashboard 可见性 + report-only 自愈建议”主线，保持不自动发送外部消息、不自动重启、不自动切路由。

### 完成内容

`atlas-provider-watch.service`：

- 新增 `notification_message(alert)`，将 provider alert 渲染为可发送文本。
- 新增 `due_alerts(...)`，统一筛选 `notification_due=true` 的活跃告警。
- 新增 `GET /api/notify-preview`：
  - `mode=dry-run`
  - 返回 `notification_due`
  - 返回待发送 `messages`
  - 明确 policy：调用方必须外部发送成功后再 mark。
- 新增 `POST /api/notify-mark-due?confirm=true`：
  - 默认不带 confirm 时只返回 `would_mark`，不改变状态。
  - 带 `confirm=true` 才批量写入 `last_notified_at`。
- Provider Watch 仍然不主动发送外部消息，只提供状态和 dry-run 预览。

`atlas-control.service` 前端：

- 导航新增 `自检` 页。
- `refresh()` 同步拉取 `/api/self-check`。
- 新增 `remediationView()`：展示 self-check summary、recall probe、findings、remediation/actionability/command/evidence。
- 总览页新增 `自检建议` 区块：展示 severe/warning/remediation 计数和前 3 条 finding。
- 所有 remediation command 都以只读文本展示，不做执行按钮。

### 验证

- 备份：
  - `/opt/atlas-provider-watch/backups/main.py.pre-notify-preview-20260626T100420Z`
  - `/opt/atlas-control/backups/index.html.pre-remediation-ui-20260626T100420Z`
- `python3 -m py_compile /opt/atlas-provider-watch/main.py` 通过。
- Atlas Control inline JS `new Function(...)` 通过。
- `atlas-provider-watch.service`、`atlas-control.service` 重启后 active。
- `GET /api/notify-preview` 200，当前 `messages=[]`，因为告警已 silence，符合预期。
- `POST /api/notify-mark-due` 不带 confirm 返回 `ok=false`、`mode=report-only`、`would_mark=[]`，符合安全预期。
- `GET /api/alerts` 200。
- Control 页面指纹存在：`data-v="selfcheck"`、`function remediationView`、`function selfCheckInline`、`自检建议`。
- `GET /api/self-check` 200：10 个 Atlas 服务 online，critical=0，warning=1，report_only_remediations=1。
- `nginx -t` 在阶段 3g 已验证 successful，本阶段未改 nginx 配置。

### 当前状态

Provider 告警具备完整 report-only 消费链路：

1. `/api/summary` 或 `/api/alerts` 产出 `notification_due`。
2. `/api/notify-preview` dry-run 渲染消息。
3. 外部消费者发送成功后，调用单条 `notify-mark` 或批量 `notify-mark-due?confirm=true`。
4. `ack/silence` 可抑制噪音。

Control Dashboard 已能直接看到自愈巡检候选建议，且不会误导用户自动执行。

### 下一步

- 给 Atlas Collab/任务队列增加执行前预检：任务运行前读取 `/api/self-check` 与 `/api/recall-probe`，如 critical>0 则阻止或标注风险。
- 可选：把 notification_due 消费器做成独立 CLI/cron job，但默认 dry-run，需显式配置目标通道。

## 2026-06-26 阶段 3i：Atlas Collab 队列执行前预检

继续路线图“任务队列 + 自检 + 自动召回闭环”主线，给 Atlas Collab queue worker 增加执行前 preflight，避免在系统 critical 状态下继续跑后台任务。

### 完成内容

`atlas-collab.service` 后端：

- `queue_jobs` 表向后兼容新增字段：
  - `preflight_json TEXT NOT NULL DEFAULT '{}'`
  - `risk_level TEXT NOT NULL DEFAULT 'unknown'`
- `_queue_row()` 返回新增：
  - `preflight`
  - `risk_level`
- 新增 `CONTROL_URL` 环境变量，默认 `http://127.0.0.1:19600`，便于测试和未来迁移。
- 新增 `_preflight(job)`：
  - 读取 `Atlas Control /api/self-check`
  - 读取 `Atlas Control /api/recall-probe?q=...`
  - 计算 `risk_level=ok|warning|critical|skipped`
  - 收集 `reasons`
  - critical 且未显式 `payload.allow_critical=true` 时 `blocked=true`
- 新增 `_store_preflight(job_id, preflight)`：
  - 写入 `preflight_json`
  - 写入 `risk_level`
  - 写入 job log：`preflight risk=... blocked=...`
- `_run_job()` 开头执行 preflight：
  - 若 `blocked=true` 抛出 `PreflightBlocked`
  - `worker_loop()` 捕获后将 job 标记为 `failed`，`result.preflight` 保留完整原因
- 新增 `GET /api/queue/preflight?q=...`，用于手动预览队列风险，不创建 job。
- `payload.skip_preflight=true` 可跳过预检，risk 标记为 `skipped`。
- `payload.allow_critical=true` 可允许 critical 继续执行，但 preflight 会记录高风险。

`atlas-collab.service` 前端：

- 队列列表新增 `Risk` 列。
- Selected Job 新增 `Preflight` 面板：展示 risk、blocked、allow_critical、reasons 和完整 preflight JSON。
- 继续保留原有 logs/result 展示，旧 job 兼容。

### 验证

- 备份：
  - `/opt/atlas-collab/backups/main.py.pre-preflight-20260626T101445Z`
  - `/opt/atlas-collab/backups/index.html.pre-preflight-20260626T101445Z`
- `python3 -m py_compile /opt/atlas-collab/main.py` 通过。
- Atlas Collab inline JS `new Function(...)` 通过。
- `atlas-collab.service` 重启后 active。
- `GET /api/queue/preflight?q=OpenClaw 不回消息` 200：当前 `risk_level=warning`、`blocked=false`、reason=`self-check warning=1`。
- 创建真实 recall job `Preflight recall verification`：
  - job 成功完成。
  - `risk_level=warning`。
  - `preflight.blocked=false`。
  - `preflight.self_check.summary.critical=0`。
  - `preflight.recall_probe.ok=true`，memories=3、notes=3、items=6、errors=0。
  - result 为 recall context，schema=`recall.context.v1`。
- 使用临时 mock Control URL 验证 critical 阻止逻辑：
  - mock `/api/self-check` 返回 `critical=1`、`ok=false`。
  - 临时 Collab worker 将 job 标记为 `failed`。
  - `risk_level=critical`。
  - `preflight.blocked=true`。
  - `result.preflight.reasons` 包含 `self-check critical=1` 和 `self-check ok=false`。
  - 测试后已停止 mock 并恢复生产 `atlas-collab.service`。
- 生产复查：
  - `atlas-collab.service`、`atlas-control.service`、`atlas-recall.service`、`atlas-provider-watch.service` 均 active。
  - `GET /api/status` queue worker running。
  - `nginx -t` successful。

### 当前状态

Atlas Collab 队列现在具备执行前安全闸：

- 正常/警告：继续执行并记录风险。
- Critical：默认阻止执行，保留完整 preflight 证据。
- 明确人工覆盖：`allow_critical=true` 可继续，但会留下风险记录。
- 所有行为仍为 report-only + 人工可审计，不自动修复、不自动重启、不自动切路由。

### 下一步

- 将 preflight 摘要接入任务创建响应或创建前预览按钮。
- 增加队列归档报告中的 preflight 摘要高亮。
- 继续推进 OpenClaw/QQ 命令与 Collab 队列联动：通过 QQ 创建 recall/self-check job 并返回 job id。

## 2026-06-26 阶段 3j：QQ 命令创建 Collab 队列 job 并回传 preflight

继续路线图“QQ 指令 + 队列联动 + 风险预检可见”主线，把 QQ 入口和 Atlas Collab 队列真正串起来。

### 完成内容

`atlas-control.service`：

- `QQ_COMMANDS` 新增：
  - `/排队记忆`
  - `/排队巡检`
- `parse_qq_command()` 支持：
  - `/排队记忆 xxx`
  - `/排队巡检`
- `qq_preview_response()` 支持对应预览文案。
- `dispatch_qq_command()` 支持：
  - `queue_recall` → 调 `Atlas Collab /api/queue/jobs` 创建 `qq-recall` job
  - `queue_self_check` → 创建 `qq-self-check` job
- QQ 执行回包中包含：
  - `reply` / `reply_preview`
  - `job`
  - `preflight`
  - `generated_at`
- 新增 `queue_job_via_collab()`，调用 Collab 队列创建接口。

`atlas-collab.service`：

- `QUEUE_KINDS` 新增：
  - `qq-recall`
  - `qq-self-check`
- `_run_job()` 兼容这两个 QQ 专用 kind，复用 existing recall/self-check 执行路径。

### 验证

- `python3 -m py_compile /opt/atlas-control/backend.py /opt/atlas-collab/main.py` 通过。
- `atlas-control.service`、`atlas-collab.service` 重启后 active。
- `GET /api/qq-commands` 200，命令列表中可见 `/排队记忆`、`/排队巡检`。
- `POST /api/qq-command` 执行 `/排队记忆 OpenClaw 不回消息`：
  - 返回 `ok=true`。
  - 创建 `qq-recall` job。
  - job 在 Collab 中完成，`risk_level=warning`，`preflight.blocked=false`。
- `POST /api/qq-command` 执行 `/排队巡检`：
  - 返回 `ok=true`。
  - 创建 `qq-self-check` job。
  - job 在 Collab 中完成，`risk_level=warning`，`preflight.blocked=false`。
- 队列 job 状态已确认：
  - `qq-self-check` done
  - `qq-recall` done
  - 两者 preflight 均记录 `self-check warning=1`，recall_probe ok。
- 生产服务 active，`nginx -t` successful。

### 当前状态

QQ 已经可以直接把“排队记忆 / 排队巡检”交给 Atlas Collab 队列执行，并把风险预检结果纳入同一条链路，满足“多 agent 协作 + 可审计 + report-only”的目标。

### 下一步

- 如果还要继续路线图，可以把 QQ 回复进一步扩展为 job 完成后的结果摘要通知。
- 或把 Collab job 的 preflight 摘要在 queue UI 里做更醒目的高亮。
- 路线图剩余项可继续按“联动 / 汇总 / 通知”方向收口。

## 2026-06-27 阶段 3k：QQ 指令菜单补齐 `/状态`、`/最近错误`、`/生成日报`

继续路线图“QQ 指令菜单 + 任务队列中心 + 管理体验增强”主线，在已有 `/查中转`、`/服务器巡检`、`/查记忆`、`/排队记忆`、`/排队巡检`、`/今日任务` 的基础上补齐更多候选指令。

### 完成内容

`atlas-control.service`：

- `QQ_COMMANDS` 新增：
  - `/状态`
  - `/最近错误`
  - `/生成日报`
- `parse_qq_command()` 支持上述命令。
- `qq_preview_response()` 为新增命令提供 dry-run 预览说明。
- `dispatch_qq_command()` 新增执行路径：
  - `/状态`：聚合 `Atlas Control /api/overview` 与 `Atlas Collab /api/status`。
  - `/最近错误`：聚合 `Atlas Control /api/self-check`、`Atlas Provider Watch /api/alerts`、`Atlas Collab failed queue jobs`。
  - `/生成日报`：创建 `Atlas Collab qq-daily` job，并返回 job id / preflight。
- 新增回复摘要 helper：
  - `reply_for_status()`
  - `reply_for_recent_errors()`
- 修复聚合命令 ok 判定：`/状态`、`/最近错误` 只要任一聚合源可用即返回 `ok=true`，避免 reply_preview 正常但 ok=false。

`atlas-collab.service`：

- `QUEUE_KINDS` 新增：
  - `daily`
  - `qq-daily`
- `_run_job()` 支持 daily/qq-daily：读取 `Atlas Daily /api/summaries`，作为队列任务结果。
- daily job 继续经过已有 preflight：critical 默认阻止，warning 继续执行并记录风险。

### 验证

- 备份：
  - `/opt/atlas-control/backups/backend.py.pre-qq-status-errors-daily-20260627T014103Z`
  - `/opt/atlas-collab/backups/main.py.pre-qq-status-errors-daily-20260627T014103Z`
- `python3 -m py_compile /opt/atlas-control/backend.py /opt/atlas-collab/main.py` 通过。
- `atlas-control.service`、`atlas-collab.service` 重启后 active。
- `POST /api/qq-command` execute `/状态`：`ok=true`，回复示例：服务 `10/10` 在线，队列 queued/running/done 摘要正常。
- `POST /api/qq-command` execute `/最近错误`：`ok=true`，返回 findings/provider alerts/failed jobs 摘要。
- `POST /api/qq-command` execute `/生成日报`：`ok=true`，创建 `qq-daily` job。
- `qq-daily` job 完成：`status=done`、`risk=warning`、`blocked=false`、结果包含 Daily summaries。
- 回归 `/排队记忆 OpenClaw 不回消息`：创建 `qq-recall` job，完成后 `risk=warning`、`blocked=false`。
- `/api/qq-commands` 当前命令列表：
  - `/状态`
  - `/查中转`
  - `/服务器巡检`
  - `/排队记忆`
  - `/排队巡检`
  - `/生成日报`
  - `/最近错误`
  - `/查记忆`
  - `/今日任务`
- `nginx -t` successful。

### 当前状态

QQ 指令菜单已覆盖路线图候选命令中的大部分低风险/可队列化能力：状态、中转、记忆、巡检、日报、任务、最近错误。高风险命令如 `/重启openclaw`、`/压测中转`、`/同步github` 仍未启用，符合安全边界：涉及重启、压测、外部写入的操作需要额外确认或候选方案，不应默认直接开放。

### 下一步建议

- 如果继续路线图，可做 `/重启openclaw` 的“候选确认/仅生成操作卡”版本，不直接执行。
- `/压测中转` 应先设计限流和 dry-run，不直接打 chat/completions。
- `/同步github` 应先做差异报告和敏感文件检查，不直接 push。

## 2026-06-27 阶段 3l：高风险 QQ 命令操作卡与只读错误摘要

继续路线图“QQ 指令菜单 + 自愈巡检 + 统一日志和可观测性”主线。本阶段补齐高风险候选命令，但严格保持 report-only：不自动重启、不自动压测、不自动 GitHub push。

### 完成内容

`atlas-control.service`：

- `QQ_COMMANDS` 新增高风险候选命令的安全入口：
  - `/重启openclaw`
  - `/压测中转`
  - `/同步github`
- 新增 `action_plan(kind)`：生成操作卡，不执行任何变更。
- 新增 `reply_for_action_plan()`：QQ 回包明确标注：
  - `mode=report-only`
  - `execute=false`
  - `confirmation_required=true`
  - 风险等级
- 新增 API：
  - `GET /api/action-plan/restart-openclaw`
  - `GET /api/action-plan/provider-stress`
  - `GET /api/action-plan/github-sync`
- `/重启openclaw` 操作卡只给出状态/日志/配置检查步骤和建议命令，不执行 restart。
- `/压测中转` 操作卡只给出限流方案，默认不调用 `/chat/completions`，避免触发 key 风控。
- `/同步github` 操作卡只给出 diff、secret scan、changelog/PR 草稿思路，不执行 push。

### 统一日志/最近错误增强

- 新增只读 API：`GET /api/error-summary?lines=80`
- 从 Atlas 服务白名单的 `systemd journal` 读取最近日志并做错误类型聚合：
  - traceback / exception / error
  - timeout
  - HTTP 5xx
  - auth 401/403
  - connection refused/reset/URLError
  - preflight/blocked
- 输出做基础脱敏。
- `/最近错误` 已接入 `error_summary`，回复中增加 `journal matches`。

### 验证

- 备份：
  - `/opt/atlas-control/backups/backend.py.pre-qq-highrisk-plan-20260627T015409Z`
  - `/opt/atlas-control/backups/backend.py.pre-error-summary-20260627T020108Z`
- `python3 -m py_compile /opt/atlas-control/backend.py` 通过。
- `atlas-control.service` 重启后 active。
- 高风险操作卡验证：
  - `/api/action-plan/restart-openclaw` → `mode=report-only`、`execute=false`、`confirmation_required=true`、`risk=medium`
  - `/api/action-plan/provider-stress` → `mode=report-only`、`execute=false`、`confirmation_required=true`、`risk=high`
  - `/api/action-plan/github-sync` → `mode=report-only`、`execute=false`、`confirmation_required=true`、`risk=medium`
- QQ 命令验证：
  - `/重启openclaw` → `ok=true`，返回操作卡，不重启。
  - `/压测中转` → `ok=true`，返回限流预案，不压测。
  - `/同步github` → `ok=true`，返回同步前检查预案，不 push。
- `/api/qq-commands` 当前包含 12 条命令：
  - `/状态`
  - `/查中转`
  - `/服务器巡检`
  - `/排队记忆`
  - `/排队巡检`
  - `/生成日报`
  - `/最近错误`
  - `/重启openclaw`
  - `/压测中转`
  - `/同步github`
  - `/查记忆`
  - `/今日任务`
- `GET /api/error-summary?lines=60` 返回 `ok=true`、`mode=read-only`。
- `/最近错误` 已返回 journal matches，并包含 `error_summary` 数据。
- `nginx -t` successful。

### 当前状态

路线图 QQ 指令菜单候选项已全部覆盖；其中低风险命令可直接只读/队列化执行，高风险命令只生成操作卡，仍需人工确认，符合安全边界。

## 2026-06-27 阶段 3m：Hermes Gateway 专属控制补齐

用户追问“Hermes 方面的控制呢”。此前 Atlas Control 已统一观察 Atlas 服务与部分 OpenClaw 操作卡，但 Hermes Gateway 仍只作为 systemd 服务存在，缺少专属只读状态和操作卡。本阶段补齐 Hermes 侧控制能力，同时保持 report-only 安全边界。

### 完成内容

`atlas-control.service`：

- 新增只读 Hermes 状态接口：
  - `GET /api/hermes/status?lines=100`
- Hermes 状态读取内容：
  - `hermes-gateway.service` systemd 状态
  - MainPID
  - psutil 进程状态 / RSS 内存 / CPU / 线程数
  - 最近 journal warning/error 摘要
  - 最近 QQBot / WebSocket / slash command 相关日志摘要
  - 输出脱敏
- 新增 Hermes 重启操作卡：
  - `GET /api/action-plan/restart-hermes`
  - `mode=report-only`
  - `execute=false`
  - `confirmation_required=true`
  - 不执行 restart
- QQ 命令新增：
  - `/hermes状态`
  - `/重启hermes`
- `/hermes状态` 返回 Hermes Gateway active/pid/内存/warning/QQBot events 摘要。
- `/重启hermes` 返回 Hermes Gateway 重启前检查卡，不直接重启。

### 验证

- 备份：`/opt/atlas-control/backups/backend.py.pre-hermes-control-20260627T021830Z`
- `python3 -m py_compile /opt/atlas-control/backend.py` 通过。
- `atlas-control.service` 重启后 active。
- `GET /api/hermes/status?lines=80` 返回 `ok=true`、`mode=read-only`，当前 `hermes-gateway.service` active/running。
- `GET /api/action-plan/restart-hermes` 返回 `ok=true`、`mode=report-only`、`execute=false`、`risk=medium`。
- QQ `/hermes状态` 返回 `ok=true` 和 Hermes 状态摘要。
- QQ `/重启hermes` 返回 `ok=true` 和 report-only 操作卡，未执行重启。
- `/api/qq-commands` 已包含 `/hermes状态`、`/重启hermes`。
- `nginx -t` successful。

### 当前状态

Hermes 侧控制已补齐为：只读状态 + 日志摘要 + report-only 重启操作卡。OpenClaw/Hermes/Atlas 的危险控制均保持“先生成操作卡，人工确认后再执行”的模式。

## 2026-06-27 阶段 3n：Web 搜索 / 网页读取研究队列

继续路线图第 3 项“Web 搜索能力”与第 11 项“任务队列中心”。本阶段实现只读 Web 研究 job：不登录、不使用浏览器、不提交表单、不外部写入；仅发送搜索查询和公开网页 GET 请求，结果进入 Collab 队列结果和本地 Obsidian 报告。

### 完成内容

`atlas-collab.service`：

- `QUEUE_KINDS` 新增：
  - `web-research`
  - `qq-web-research`
- 新增 `_run_web_research(payload)`：
  - 使用 DuckDuckGo HTML 做只读搜索。
  - 对搜索结果做只读 GET 抓取。
  - 不带 Cookie / Authorization。
  - 不登录、不点击、不提交表单、不使用浏览器 profile。
  - 限制结果数、抓取页数、单页读取字节、snippet 字符数。
  - 生成 `safety_profile=web-research.v1`。
  - 结果包含：`external_network=true`、`external_write=false`、`methods_allowed=[GET]`、`login_allowed=false`、`form_submit_allowed=false`、`browser_used=false`。
- 研究报告保存到本地：
  - `/root/obsidian-vault/agents/reports/atlas-queue/research/`
  - 报告 frontmatter 标注 read-only。
  - 报告内容脱敏。
  - 返回 `report_path` 与 `report_sha256`。
- 增加队列 kind 白名单校验：未知 `kind` 创建 job 会返回 400。
- 增加外部 URL SSRF 防护：
  - 仅允许 http/https。
  - 禁止 userinfo。
  - 禁止 localhost、`.local`、metadata host。
  - 禁止非标准端口。
  - DNS 解析后阻止 private/loopback/link-local/reserved/unspecified/multicast IP。
  - redirect 后重新校验 final URL。

`atlas-control.service`：

- QQ 命令新增：
  - `/研究 xxx`
- `/api/qq-commands` 对 `/研究` 暴露风险元数据：
  - `risk=external-read`
  - `mode=report-only`
  - `external_network=true`
  - `external_write=false`
  - `browser_used=false`
  - `login_allowed=false`
  - `form_submit_allowed=false`
  - `methods_allowed=[GET]`
  - forbidden targets 与 limits
- `/研究` 会创建 `qq-web-research` Collab job。
- 查询词疑似包含 `api_key/token/secret/password/authorization/cookie`、Bearer、`sk-*` 时阻止，避免把敏感内容发给搜索引擎。

### 验证

- 备份：
  - `/opt/atlas-collab/backups/main.py.pre-web-research-20260627T022926Z`
  - `/opt/atlas-control/backups/backend.py.pre-web-research-20260627T022926Z`
- `python3 -m py_compile /opt/atlas-control/backend.py /opt/atlas-collab/main.py` 通过。
- `atlas-control.service`、`atlas-collab.service` 重启后 active。
- `/api/qq-commands` 可见 `/研究`，并返回风险元数据：`external-read`、`external_network=true`、`methods=[GET]`。
- 敏感查询阻断：`/研究 api_key=sk-...` 返回 `sensitive_query_blocked`。
- 未知队列 kind 被拒绝：POST `/api/queue/jobs` kind=`bad-kind` 返回 HTTP 400。
- `/研究 OpenClaw memory RAG` 成功创建 `qq-web-research` job。
- job 完成：`status=done`、`risk=warning`、`blocked=false`、`safety_profile=web-research.v1`、`external_write=false`、本地报告存在。
- 回归 `/状态`：`ok=true`。
- `nginx -t` successful。

### 备注

本次 DuckDuckGo HTML 对测试 query 返回 0 条解析结果，但 job 流程、安全边界、报告生成和队列状态均通过。后续可继续增强搜索 provider fallback（例如 Brave/Perplexity/API 化搜索），仍需保持只读和外发提示。

## 2026-06-27 阶段 3o：模型路由 dry-run 推荐层

继续路线图第 7 项“模型路由系统”。本阶段只实现 report-only 推荐层：根据任务描述给出模型/路由建议，不修改 provider 配置，不自动切换 Hermes/OpenClaw 主回复路由。

### 完成内容

`atlas-control.service`：

- 新增 API：`GET /api/route-plan?task=...`
- 新增 QQ 命令：`/模型建议 xxx`
- 路由建议分类：
  - `high_risk_ops`：服务器/配置/密钥/删除/数据库等高风险操作。
  - `coding`：代码实现/调试。
  - `research`：资料检索/研究。
  - `memory`：记忆召回/知识库任务。
  - `summarize`：摘要/报告任务。
  - `general`：通用任务。
- 推荐策略：
  - 高风险操作 → 强推理远程主模型 + 人工确认。
  - 代码任务 → 强推理代码模型/主模型，可用子 agent 审查。
  - 研究任务 → 队列化 web-research + 主模型综合。
  - 记忆任务 → Atlas Recall first。
  - 摘要任务 → 可用低风险辅助模型草稿，主模型审定。
- 输出 provider context：targets 数、healthy 数、alert targets。
- 输出 cautions：
  - `report-only`
  - 不修改 provider 配置
  - 不自动切换主路由
  - provider alert 存在时提醒先 `/查中转`

### 验证

- 备份：`/opt/atlas-control/backups/backend.py.pre-route-plan-20260627T024339Z`
- `python3 -m py_compile /opt/atlas-control/backend.py` 通过。
- `atlas-control.service` 重启后 active。
- `GET /api/route-plan?task=修复 Atlas Control nginx 配置并重启服务` 返回：
  - `ok=true`
  - `mode=report-only`
  - `execute=false`
  - `category=high_risk_ops`
  - `route=strong-remote-main`
  - 当前 provider alert：`sub-zmjjkkk`
- QQ `/模型建议 修复 Atlas Control nginx 配置并重启服务` 返回强推理远程主模型 + 人工确认建议，不自动切换。
- QQ `/模型建议 总结昨天的日报` 返回摘要类低风险辅助模型草稿 + 主模型审定建议，不自动切换。
- `/api/qq-commands` 已包含 `/模型建议`。
- `nginx -t` successful。

### 当前状态

路线图“模型路由系统”已完成安全的建议层。自动切换主回复路由、本地模型降级、高并发路由测试仍不启用，继续保持 report-only。

## 2026-06-27 阶段 3p：任务队列完成通知预览与失败恢复建议

继续路线图第 11 项“任务队列中心”，补齐长任务完成后的通知预览和失败恢复建议。本阶段保持安全边界：不主动发送 QQ/Telegram/Signal，不外部通知，只生成 dry-run preview 和 report-only 建议。

### 完成内容

`atlas-collab.service`：

- 新增只读/预览端点：
  - `GET /api/queue/jobs/{job_id}/notify-preview`
  - `GET /api/queue/jobs/{job_id}/recovery-suggestion`
- `notify-preview`：
  - `mode=dry-run`
  - `external_send=false`
  - 不发送任何外部通知
  - 生成 would-send 消息，包括 job id、title、status、risk、blocked、result 摘要、report path/error 等。
- `recovery-suggestion`：
  - `mode=report-only`
  - `execute=false`
  - 对 failed / blocked / timeout / unsupported kind / safety block / web-research 等场景生成恢复建议。
  - 对 done job 返回 no recovery needed。

`atlas-control.service`：

- 新增代理端点：
  - `GET /api/queue/jobs/{job_id}/notify-preview`
  - `GET /api/queue/jobs/{job_id}/recovery-suggestion`
- 统一从 Atlas Control 入口读取队列通知预览/恢复建议。

### 验证

- 备份：
  - `/opt/atlas-collab/backups/main.py.pre-queue-notify-recovery-20260627T025757Z`
  - `/opt/atlas-control/backups/backend.py.pre-queue-notify-recovery-20260627T025858Z`
- `python3 -m py_compile /opt/atlas-collab/main.py /opt/atlas-control/backend.py` 通过。
- `atlas-collab.service`、`atlas-control.service` 重启后 active。
- Collab 直接验证：
  - `/api/queue/jobs/{id}/notify-preview` 返回 `ok=true`、`mode=dry-run`、`external_send=false`、done job `would_send=true`。
  - `/api/queue/jobs/{id}/recovery-suggestion` 返回 `ok=true`、`mode=report-only`，done job 建议 `No recovery action needed`。
- Control 代理验证：
  - `/api/queue/jobs/{id}/notify-preview` 返回 `ok=true`、`mode=dry-run`。
  - `/api/queue/jobs/{id}/recovery-suggestion` 返回 `ok=true`、`mode=report-only`。
- `nginx -t` successful。

### 当前状态

任务队列中心现在具备：创建/执行/重试/取消/归档/preflight/risk/完成通知预览/失败恢复建议。真正主动通知仍未启用，符合默认不外发的安全策略。
