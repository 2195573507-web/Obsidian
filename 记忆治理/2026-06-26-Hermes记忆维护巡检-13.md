# Hermes 记忆维护巡检（2026-06-26 13:00 BJT）

## 健康状态
- `shared-memory-client health`：`ok=true`
- 记忆总数：58，事件：0，向量：55，本地任务：39
- 导出与重建已执行：`export` / `reindex` / `embed --limit 200` 均成功

## 各层数量
- `service_state`：12 总计，11 活跃
- `decision`：10 总计，4 活跃
- `fact`：10 总计，9 活跃
- `success`：9 总计，8 活跃
- `temporary`：8 总计，0 活跃
- `failure`：3 总计，2 活跃
- `preference`：3 总计，1 活跃
- `noise`：2 总计，1 活跃
- `todo`：1 总计，1 活跃

## 候选问题
- debug 召回仍包含旧的 qwen/千问相关决策和时间规则历史条目，虽然它们看起来是有意保留的历史状态，但与当前“已移除千问/当前不使用 qwen”的运行态存在并列展示，建议人工确认是否需要继续保留为活跃决策。
- 当前 `expire` 未禁用任何条目，未发现自动过期动作。

## 处理建议
- 无需自动删除、禁用或合并；如需进一步整理，请人工确认后再处理。
- 本次巡检结果总体正常，无须紧急处理。
