# Hermes 共享/长期记忆维护巡检

- 巡检时间（BJT）：2026-07-02 13:00:22 CST (+0800)
- 执行身份：Hermes cron（共享/长期记忆维护）
- 安全边界：仅健康检查、层统计、expire、export、reindex、embed 与候选报告；未删除、未禁用、未合并重要记忆；未修改 Hermes/OpenClaw 本地 MEMORY/USER；未整理 skills；未写入凭证明文。

## 健康状态

```json
{
  "ok": true,
  "version": "1.3.0",
  "memories": 81,
  "events": 0,
  "embeddings": 78,
  "local_tasks": 39,
  "db": "/opt/shared-agent-memory/data/memory.sqlite",
  "vault": "/root/obsidian-vault/shared-memory",
  "models": {
    "classifier": null,
    "embedding": "nomic-embed-text",
    "local_worker": null
  }
}
```

## Layers 统计

| kind | count | active | expired |
|---|---:|---:|---:|
| success | 30 | 29 | 0 |
| service_state | 12 | 11 | 0 |
| decision | 11 | 5 | 0 |
| fact | 11 | 10 | 0 |
| temporary | 8 | 0 | 0 |
| failure | 3 | 2 | 0 |
| preference | 3 | 1 | 0 |
| noise | 2 | 0 | 0 |
| todo | 1 | 1 | 0 |

## 本次维护动作

- `expire`：成功，disabled=0。
- `context --mode debug`：成功返回；可见当前主导记忆包含“不再使用千问/qwen”“移除本地千问”“Ollama 仅保留 nomic-embed-text”等高重要度记录。
- `export`：成功，导出路径 `/root/obsidian-vault/shared-memory/exports/shared-memory.md`。
- `reindex`：成功；本次嵌入新增 0，失败 0。
- `embed --limit 200`：成功；embedded=0，failed=0。

## 候选问题（仅报告，不自动处理）

1. **调度规则候选冲突**：debug context 中仍有一条 2026-06-23 决策称“删除/废弃晚上不允许做任务、避免 BJT 00:00-07:00 创建任务的旧规则”，但当前维护任务说明要求尊重 BJT 调度约定并避免 00:00-07:00。建议人工确认不同任务域的适用范围，必要时新增更精确的调度边界说明。
2. **模型路由/千问候选冲突**：当前共享记忆高优先级记录显示服务器“不再使用千问/qwen”，health 也显示 classifier/local_worker 为 null、embedding 为 nomic-embed-text；但已加载的 `shared-agent-memory` 技能文本仍描述 `add-auto` 使用 qwen2.5:3b。建议人工后续核对技能文档是否需要更新，避免旧说明误导。
3. **学习报告候选重复**：debug context 中可见多条“每日开源学习 2026-06-26”success 记忆，内容有重叠。未自动合并；如需瘦身，可人工确认后将其合并为一条日期级摘要并禁用旧条目。
4. **OpenClaw skill_workshop 待办仍存在**：仍有 1 条 todo，关于 pending skill proposals 等待受支持审批路径；不属于本次自动处理范围。

## 是否需要人工确认

- 系统健康：正常。
- 需要立即人工处理：否。
- 可选人工确认：上述 4 类候选问题，尤其是调度规则边界与技能文档中的 qwen 旧描述。
