# Hermes 共享/长期记忆维护巡检

- 巡检时间：2026-06-30 17:00:15 CST (+0800)
- 执行身份：Hermes cron（共享/长期记忆维护）
- 安全边界：仅执行健康检查、layers、expire、context debug、export、reindex、embed 与候选报告；未删除、禁用、合并记忆；未修改 Hermes/OpenClaw 本地 MEMORY/USER；未整理 skills；未写入凭证明文。

## 健康状态

```json
{
  "ok": true,
  "version": "1.3.0",
  "memories": 80,
  "events": 0,
  "embeddings": 77,
  "local_tasks": 39,
  "db": "/opt/shared-agent-memory/data/memory.sqlite",
  "vault": "/root/obsidian-vault/shared-memory",
  "models": {
    "classifier": null,
    "embedding": "nomic-embed-text",
    "local_worker": null
  }
}
```

## Layers 统计

| kind | count | active | expired |
|---|---:|---:|---:|
| success | 29 | 28 | 0 |
| service_state | 12 | 11 | 0 |
| decision | 11 | 5 | 0 |
| fact | 11 | 10 | 0 |
| temporary | 8 | 0 | 0 |
| failure | 3 | 2 | 0 |
| preference | 3 | 1 | 0 |
| noise | 2 | 0 | 0 |
| todo | 1 | 1 | 0 |

## 维护动作结果

- `expire`：成功，disabled=0。
- `context --mode debug`：成功返回偏好/事实/服务状态/决策/成功/失败/todo/noise 摘要。
- `export`：成功，导出路径 `/root/obsidian-vault/shared-memory/exports/shared-memory.md`。
- `reindex`：成功。
- `embed --limit 200`：成功，embedded=0，failed=0（当前无待嵌入项）。

## 候选问题（仅报告，不自动处理）

1. **模型/本地任务记忆需人工关注但不需立即处理**：当前健康状态显示 `classifier=null`、`local_worker=null`，debug context 中也有 2026-06-23/24 的高优先级决策说明“不再使用千问/qwen，仅保留 nomic-embed-text”。但共享记忆技能说明中仍保留 `add-auto` 使用 qwen2.5 分类的旧工作流描述。建议人工确认文档/技能说明是否需要同步更新；本巡检未修改技能。
2. **夜间/凌晨任务规则存在历史表述差异**：debug context 中有高优先级决策称用户已废弃“避免 BJT 00:00-07:00 创建任务”的旧规则；当前本巡检任务自身明确只在 BJT 09:00-23:00 运行且不要新建 cron。建议后续只按具体任务说明执行，不自动套用旧规则。
3. **长期 todo 仍存在**：OpenClaw pending skill proposals 仍有待支持审批路径后处理；本巡检未绕过审批，也未安装/整理 skills。

## 是否需要人工确认

- 当前服务健康，导出/索引/嵌入均成功；无须紧急人工处理。
- 若要提升记忆一致性，建议人工确认是否更新共享记忆技能中关于 qwen/add-auto 的描述，以及是否继续保留 pending skill proposals 的 todo。
