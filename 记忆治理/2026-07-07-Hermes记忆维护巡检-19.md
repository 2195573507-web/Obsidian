# Hermes 共享记忆维护巡检 - 2026-07-07 19:00 BJT

## 基本信息

- 巡检时间：2026-07-07 19:00:40 CST
- 数据库：`/opt/shared-agent-memory/data/memory.sqlite`
- 导出文件：`/root/obsidian-vault/shared-memory/exports/shared-memory.md`
- 维护边界：仅执行 health/layers/expire/export/reindex/embed 与候选报告；未删除、禁用、合并重要记忆；未修改 Hermes/OpenClaw 本地 MEMORY/USER；未整理 skills。

## 健康状态

- `shared-memory-client health`：ok=true
- 版本：1.3.0
- memories：85
- events：0
- embeddings：82
- local_tasks：39
- 模型状态：classifier=null；embedding=`nomic-embed-text`；local_worker=null

## Layers 统计

| kind | count | active | expired |
|---|---:|---:|---:|
| success | 34 | 33 | 0 |
| service_state | 12 | 11 | 0 |
| decision | 11 | 5 | 0 |
| fact | 11 | 10 | 0 |
| temporary | 8 | 0 | 0 |
| failure | 3 | 2 | 0 |
| preference | 3 | 1 | 0 |
| noise | 2 | 0 | 0 |
| todo | 1 | 1 | 0 |

## 本次维护结果

- `expire`：ok=true，disabled=0
- `export`：ok=true，导出到 `/root/obsidian-vault/shared-memory/exports/shared-memory.md`
- `reindex`：ok=true，embedded=0，failed=0
- `embed --limit 200`：ok=true，model=`nomic-embed-text`，embedded=0，failed=0

## 候选问题

1. 调度规则存在候选冲突：debug context 中仍有一条高重要 decision 表示“删除/废弃晚上不允许做任务、避免 BJT 00:00-07:00 创建任务”的旧限制；而当前共享记忆维护任务要求遵守 BJT 09:00-23:00 轮转并避免 00:00-07:00。建议人工确认该旧 decision 是否需要降权、补充范围说明或禁用。
2. qwen/千问状态存在文档层候选冲突：debug context 与 health 均显示当前 classifier/local_worker 为 null，仅保留 `nomic-embed-text`；但已加载的 shared-agent-memory 技能说明里仍提到 `add-auto` 使用本地 Ollama `qwen2.5:3b`。建议人工确认技能文档是否需要更新为当前状态。
3. success 层包含多条每日开源学习记录，内容相近但日期不同；当前不自动合并。若后续检索噪声变高，可人工确认是否改为月度汇总或降低部分历史条目重要性。
4. temporary 层 count=8 但 active=0、noise 层 count=2 但 active=0，属于非活动历史记录；本次不处理。

## 是否需要人工确认

需要人工确认：是。建议优先确认“调度规则候选冲突”和“qwen/千问技能文档是否已过期”。
