# Hermes 共享/长期记忆维护巡检（2026-06-29 13:00 BJT）

- 巡检时间：2026-06-29 13:00:46 CST (+0800)
- 健康状态：正常（`ok=true`，版本 `1.3.0`）
- 数据库：`/opt/shared-agent-memory/data/memory.sqlite`
- 导出目录：`/root/obsidian-vault/shared-memory`
- 总记忆数：79；embeddings：76；local_tasks：39
- 模型状态：classifier=`null`，embedding=`nomic-embed-text`，local_worker=`null`

## Layers 统计

| kind | count | active | expired |
|---|---:|---:|---:|
| success | 28 | 27 | 0 |
| service_state | 12 | 11 | 0 |
| decision | 11 | 5 | 0 |
| fact | 11 | 10 | 0 |
| temporary | 8 | 0 | 0 |
| failure | 3 | 2 | 0 |
| preference | 3 | 1 | 0 |
| noise | 2 | 0 | 0 |
| todo | 1 | 1 | 0 |

## 本次维护动作

- `shared-memory-client health`：通过。
- `shared-memory-client layers`：通过。
- `shared-memory-client expire`：通过，disabled=0。
- `shared-memory-client context ... --mode debug`：完成调试召回。
- `shared-memory-client export`、`reindex`、`embed --limit 200`：完成；本轮 embed 新增 0、失败 0。

## 候选问题（未自动处理）

1. **调度规则候选冲突**：调试上下文中仍有一条高重要度 decision 表示用户已废弃“避免 BJT 00:00-07:00 创建任务”的旧规则；当前共享记忆技能的维护模式又保留“避免 BJT 00:00-07:00”的调度约定。建议人工确认哪条作为共享记忆维护任务的长期规则，但本次未修改。
2. **模型/千问历史表述候选冲突**：当前健康检查显示 classifier/local_worker 均为 `null`，且记忆中有高重要度记录说明已移除 qwen；但技能说明中仍包含 `add-auto` 使用 qwen2.5 的历史描述。建议后续人工核对技能文档是否需更新，本次未改技能。
3. **重复学习摘要候选**：调试上下文中每日开源学习相关 success 条目较多，存在同日期多条摘要并存（尤其 2026-06-26/2026-06-24）。可后续人工决定是否合并/降噪；本次仅报告，不禁用、不合并。

## 是否需要人工确认

- 需要：如要统一调度规则、更新技能文档中的 qwen 历史描述、或合并每日学习摘要，需要人工确认后再执行。
- 无需紧急处理：健康、导出、reindex、embedding 均正常；未发现本轮需要立即修复的服务故障。
