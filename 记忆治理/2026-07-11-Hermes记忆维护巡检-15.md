# Hermes 共享记忆维护巡检

- 巡检时间：2026-07-11 15:01:45 CST（BJT）
- 健康状态：OK；shared-memory-client 版本 1.3.0；记忆 100 条；嵌入 97 条；local_tasks 39；数据库 `/opt/shared-agent-memory/data/memory.sqlite`。
- 分层统计：success 45/active 44；service_state 15/active 14；fact 12/active 11；decision 11/active 5；temporary 8/active 0；failure 3/active 2；preference 3/active 1；noise 2/active 0；todo 1/active 1。
- 过期处理：`shared-memory-client expire` 成功，disabled=0。
- 导出/索引/嵌入：export 成功；reindex 成功，新增嵌入 0、失败 0；embed --limit 200 成功，新增嵌入 0、失败 0。
- 候选问题：debug context 中同时存在“允许 BJT 00:00-07:00 任务/按用户指定执行”的较新规则，以及本巡检任务说明里的“遵守当前巡检时段，不新增 cron”的边界；当前仅记录为调度规则边界候选，无自动处理。
- 人工确认：不需要立即处理；如后续要统一记忆，可人工确认是否补充一条更明确的“巡检任务只按既定 cron 运行，不主动新建 cron”的 service_state。
