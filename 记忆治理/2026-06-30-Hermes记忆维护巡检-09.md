# Hermes 共享/长期记忆维护巡检（2026-06-30 09:01 BJT）

## 健康状态

- `shared-memory-client health`：OK
- 服务版本：`1.3.0`
- 数据库：`/opt/shared-agent-memory/data/memory.sqlite`
- Vault：`/root/obsidian-vault/shared-memory`
- 记忆总数：80
- embeddings：77
- local_tasks：39
- 模型状态：classifier=`null`，embedding=`nomic-embed-text`，local_worker=`null`

## Layers 统计

| kind | count | active | expired |
|---|---:|---:|---:|
| success | 29 | 28 | 0 |
| service_state | 12 | 11 | 0 |
| decision | 11 | 5 | 0 |
| fact | 11 | 10 | 0 |
| temporary | 8 | 0 | 0 |
| failure | 3 | 2 | 0 |
| preference | 3 | 1 | 0 |
| noise | 2 | 0 | 0 |
| todo | 1 | 1 | 0 |

## 本次维护动作

- `expire`：OK，自动禁用过期条目 0 条。
- `context --mode debug`：已读取维护相关上下文。
- `export`：OK，导出到 `/root/obsidian-vault/shared-memory/exports/shared-memory.md`。
- `reindex`：OK。
- `embed --limit 200`：OK，新增 embedded=0，failed=0。

## 候选问题（仅报告，不自动处理）

1. **调度规则候选冲突**：共享记忆中有 2026-06-23 决策称“删除/废弃避免 BJT 00:00-07:00 创建任务的旧规则”，而当前巡检任务说明继续要求本任务按 BJT 09:00-23:00 运行且不新增 cron。建议人工确认：这是“本任务专属调度边界”还是需要更新通用调度偏好。
2. **模型路由/千问历史条目候选重复**：当前高优先级记忆明确“服务器不再使用 qwen/千问；仅保留 nomic-embed-text 用于 embeddings”，但支持说明和历史学习/架构条目仍含 qwen/local-task 方案描述。建议保留为历史/参考，必要时人工标注旧条目为历史，避免误召回。
3. **每日开源学习 success 条目较多**：存在多条同类学习总结，当前不影响健康；若后续召回噪声增加，可人工合并为月度/主题总结。
4. **OpenClaw skill_workshop 待办仍未解决**：仍有 1 条 active todo，等待支持的审批路径，不应绕过审批。

## 是否需要人工确认

- 当前服务健康，无失败、无新增过期处理、无嵌入失败。
- 不需要立即人工处理。
- 如要减少长期召回噪声，建议人工确认后再处理上述“调度规则”和“qwen 历史条目”候选项。
