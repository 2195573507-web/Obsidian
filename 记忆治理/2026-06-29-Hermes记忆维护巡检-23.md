# Hermes 共享/长期记忆维护巡检（2026-06-29 23:01 BJT）

## 执行范围
- 按计划执行：`health`、`layers`、`expire`、debug `context`、`export`、`reindex`、`embed --limit 200`。
- 未执行：删除记忆、禁用重要记忆、合并用户偏好、修改 Hermes/OpenClaw 本地 MEMORY/USER、整理 skills、写入凭证明文。

## 健康状态
- 服务状态：正常（`ok: true`）
- 版本：`1.3.0`
- 数据库：`/opt/shared-agent-memory/data/memory.sqlite`
- Vault：`/root/obsidian-vault/shared-memory`
- 记忆总数：79
- embeddings：76
- local_tasks：39
- 模型状态：classifier 为 `null`；embedding 使用 `nomic-embed-text`；local_worker 为 `null`

## Layers 统计
| kind | count | active | expired |
|---|---:|---:|---:|
| success | 28 | 27 | 0 |
| service_state | 12 | 11 | 0 |
| decision | 11 | 5 | 0 |
| fact | 11 | 10 | 0 |
| temporary | 8 | 0 | 0 |
| failure | 3 | 2 | 0 |
| preference | 3 | 1 | 0 |
| noise | 2 | 0 | 0 |
| todo | 1 | 1 | 0 |

## 维护结果
- `expire`：成功，自动禁用 0 条。
- `export`：成功，导出路径 `/root/obsidian-vault/shared-memory/exports/shared-memory.md`。
- `reindex`：成功。
- `embed --limit 200`：成功，本次新增 embedded 0 条、failed 0 条。

## 候选问题（仅报告，未处理）
1. **调度时间规则可能存在历史冲突**：debug context 中存在“删除/废弃夜间禁做规则、skills 整理可在 BJT 03:00 执行”的高重要决策；当前巡检任务说明仍要求本任务按 BJT 09:00-23:00 执行且不要新建 cron。建议人工确认这两类规则的适用范围，避免误用于不同任务。
2. **千问/qwen 相关说明可能仍散落在技能文档/历史记忆中**：当前健康状态显示 classifier/local_worker 均为 `null`，context 中也有“已移除 qwen，仅保留 nomic-embed-text”的高重要决策；但共享记忆技能文本仍保留部分 `qwen2.5`/`add-auto` 描述。建议人工确认是否需要更新技能说明或保留为历史兼容说明。
3. **每日开源学习成功条目较多**：未发现必须立即处理的错误，但同类 success 记忆数量较多，后续可人工评估是否需要压缩为按日期/主题的摘要型记忆。

## 是否需要人工确认
- 当前无健康异常，无失败 embedding，无过期禁用动作。
- 需要人工确认的事项：以上 3 个候选问题均属于“可能冲突/重复/可压缩”，本任务未自动删除、禁用或合并。
