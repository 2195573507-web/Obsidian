# Hermes 共享/长期记忆维护巡检

- 巡检时间：2026-07-13 15:04:26 BJT
- 执行方：Hermes cron
- 任务边界：仅健康检查、layers 统计、expire、export、reindex、embed、生成候选报告；未删除、禁用、合并任何重要记忆；未修改 Hermes/OpenClaw 本地 MEMORY/USER；未整理 skills；未写入凭证明文。

## 健康状态

```json
{
  "ok": true,
  "version": "1.3.0",
  "memories": 103,
  "events": 0,
  "embeddings": 100,
  "local_tasks": 39,
  "db": "/opt/shared-agent-memory/data/memory.sqlite",
  "vault": "/root/obsidian-vault/shared-memory",
  "models": {
    "classifier": null,
    "embedding": "nomic-embed-text",
    "local_worker": null
  }
}
```

## Layers 统计

| kind | count | active | expired |
| --- | ---: | ---: | ---: |
| success | 48 | 47 | 0 |
| service_state | 15 | 14 | 0 |
| fact | 12 | 11 | 0 |
| decision | 11 | 5 | 0 |
| temporary | 8 | 0 | 0 |
| failure | 3 | 2 | 0 |
| preference | 3 | 1 | 0 |
| noise | 2 | 0 | 0 |
| todo | 1 | 1 | 0 |

## 维护命令结果

- `expire`：ok，disabled=0。
- `export`：ok，导出路径 `/root/obsidian-vault/shared-memory/exports/shared-memory.md`。
- `reindex`：ok，reindex 阶段返回 embeddings embedded=0 failed=0。
- `embed --limit 200`：ok，model=`nomic-embed-text`，embedded=0，failed=0。

## 候选问题

1. debug context 中同时召回了旧的调度决策：“允许北京时间 00:00-07:00 创建任务/skills 整理任务可在 03:00 执行”，而本次巡检任务明确要求遵守当前运行时间为 BJT 09:00-23:00 且不要创建新 cron。该条不影响本次执行，但建议人工确认长期记忆中调度规则的优先级与适用范围，避免未来维护任务误用旧规则。
2. debug context 中仍保留“当前不再使用 qwen/千问”和“Ollama 仅保留 nomic-embed-text 用于 embeddings”的高优先级决策；health 也显示 classifier/local_worker 为 null，状态一致，无需处理。
3. temporary/noise 当前 active=0；未发现需要自动处理的过期活跃条目。

## 是否需要人工确认

- 服务健康、导出、索引、嵌入均正常：无须人工处理。
- 可选人工确认：仅上述调度规则候选冲突是否需要后续合并/禁用旧记忆；本任务未直接处理。
