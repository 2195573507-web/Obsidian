# Hermes 共享/长期记忆维护巡检

- 巡检时间：2026-06-27 11:00:58 CST (+0800)
- 执行来源：Hermes cron，共享记忆维护巡检
- 操作边界：仅健康检查、layers、expire、context debug、export、reindex、embed、候选报告；未删除、未禁用、未合并重要记忆。

## 健康状态

```json
{
  "ok": true,
  "version": "1.3.0",
  "memories": 75,
  "events": 0,
  "embeddings": 72,
  "local_tasks": 39,
  "db": "/opt/shared-agent-memory/data/memory.sqlite",
  "vault": "/root/obsidian-vault/shared-memory",
  "models": {
    "classifier": null,
    "embedding": "nomic-embed-text",
    "local_worker": null
  }
}
```

## Layers 统计

| kind | count | active | expired |
| --- | ---: | ---: | ---: |
| success | 24 | 23 | 0 |
| service_state | 12 | 11 | 0 |
| decision | 11 | 5 | 0 |
| fact | 11 | 10 | 0 |
| temporary | 8 | 0 | 0 |
| failure | 3 | 2 | 0 |
| preference | 3 | 1 | 0 |
| noise | 2 | 1 | 0 |
| todo | 1 | 1 | 0 |

## 维护结果

- `expire`：ok，disabled=0。
- `export`：已执行。
- `reindex`：ok，export=`/root/obsidian-vault/shared-memory/exports/shared-memory.md`，embedded=0，failed=0。
- `embed --limit 200`：ok，model=`nomic-embed-text`，embedded=0，failed=0。

## 候选问题

- 候选冲突：debug context 同时出现“用户已删除夜间禁做规则 / skills 可在 BJT 03:00 执行”的高优先级决策，以及当前维护技能中的“避免 BJT 00:00-07:00”调度约束。当前任务明确运行在 BJT 09:00-23:00，不影响本次执行；如需统一长期调度规则，建议人工确认后再更新记忆/技能。
- 候选重复/历史噪声：debug context 中仍可召回多条历史 qwen/千问相关架构说明，但同时存在高优先级当前状态“服务器不再使用千问，仅保留 nomic-embed-text”。目前未直接处理；如影响召回质量，建议人工确认后禁用或降权旧条目。
- 候选膨胀：每日开源学习 success 条目较多，可能在通用 debug context 中占用篇幅。当前不自动合并；如用户确认，可生成候选归并清单。
- 状态观察：temporary count=8、active=0、expired=0，可能为已禁用或非活跃历史临时条目；本次不处理。

## 是否需要人工确认

- 本次服务健康，维护命令成功，无须立即人工处理。
- 若要优化召回质量，建议人工确认上述“调度规则冲突候选”和“历史 qwen 条目清理候选”。
