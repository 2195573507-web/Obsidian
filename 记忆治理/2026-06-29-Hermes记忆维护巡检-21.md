# Hermes 共享/长期记忆维护巡检（2026-06-29 21:00 BJT）

- 巡检时间：2026-06-29 21:00:56 CST（BJT）
- 健康状态：OK；shared-memory 服务版本 1.3.0；数据库 `/opt/shared-agent-memory/data/memory.sqlite`；导出目录 `/root/obsidian-vault/shared-memory`。
- 总量概览：memories 79；embeddings 76；local_tasks 39；events 0。
- 分层统计：success 28/active 27；service_state 12/active 11；decision 11/active 5；fact 11/active 10；temporary 8/active 0；failure 3/active 2；preference 3/active 1；noise 2/active 0；todo 1/active 1。
- expire 结果：disabled 0，无新增自动过期禁用。
- 导出/索引/嵌入：export 成功；reindex 成功并返回 embedded 0 failed 0；embed --limit 200 成功并返回 embedded 0 failed 0。

## 候选问题（仅报告，不自动处理）

1. **调度规则候选冲突**：debug context 中仍有高重要度 decision 表示用户在 2026-06-23 删除“避免 BJT 00:00-07:00 创建任务”的旧规则；而当前共享记忆维护 skill 的轮转模式要求“尊重 BJT 调度惯例：避免 BJT 00:00-07:00”。这可能是任务域特定规则与全局规则并存，建议人工确认是否需要补一条更精确的作用域说明。
2. **qwen/千问历史条目较多**：当前高重要度 decision/service_state 已说明“不再使用千问，Ollama 仅保留 nomic-embed-text 用于 embeddings”；但 skill 文档与旧 memory 中仍出现 qwen2.5/classifier/local-task 相关历史说明。建议人工确认是否需要将相关旧条目标注为历史配置或补充“当前状态优先级”摘要。
3. **每日开源学习 success 条目重复度较高**：2026-06-24 至 2026-06-29 多条 success 主题相近，可能影响 context 噪声；建议人工确认是否需要后续生成合并候选清单。
4. **OpenClaw skill_workshop 待办仍存在**：todo 仍指向 pending proposals，属于未完成事项；不自动处理，等待支持的审批路径或用户明确指令。

## 是否需要人工确认

- 需要人工确认：是否收敛调度规则作用域、qwen 历史说明、每日学习条目重复候选。
- 本次未删除、未禁用、未合并任何重要记忆；未修改 Hermes/OpenClaw 本地 MEMORY/USER；未整理 skills；未写入凭证明文。
