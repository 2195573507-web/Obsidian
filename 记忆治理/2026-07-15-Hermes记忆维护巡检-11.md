# Hermes 记忆维护巡检报告

- 巡检时间：2026-07-15 11:01 BJT
- 状态：健康
- 服务：`shared-memory-client health` 返回 `ok=true`，版本 `1.3.0`
- 数据规模：memories `103`，embeddings `100`，local_tasks `39`

## 层级统计
- `success`：48，总体活跃 47
- `service_state`：15，总体活跃 14
- `fact`：12，总体活跃 11
- `decision`：11，总体活跃 5
- `temporary`：8，当前活跃 0
- `failure`：3，当前活跃 2
- `preference`：3，当前活跃 1
- `noise`：2，当前活跃 0
- `todo`：1，当前活跃 1

## 候选问题
1. 调试上下文里仍可召回一条旧的定时规则，内容写明“允许北京时间 00:00-07:00 的任务”，这和当前巡检任务边界不一致，建议人工确认是否应作为历史规则降权或保留。
2. 上下文中同时存在多条 ESP 服务器召回条目，信息重叠度较高，建议后续人工判断是否需要合并为更紧凑的 service_state 记录。
3. 当前仍有 1 条 `todo` 活跃项：OpenClaw 相关 skill proposal 待处理，属于未完成工作项，建议继续保留但定期复核。

## 本次处理结果
- `expire`：完成，未禁用任何条目
- `export`：完成，导出文件已更新
- `reindex`：完成
- `embed --limit 200`：完成，embedded `0`，failed `0`

## 是否需要人工确认
- 需要。主要是第 1、2 项属于长期记忆层面的候选冲突/重复，按流程不直接修改，只建议后续人工复核。
