# Hermes 共享/长期记忆维护巡检

- 巡检时间（BJT）：2026-06-27 17:00:30 CST (+0800)
- 执行范围：health、layers、expire、debug context、export、reindex、embed；未删除、未禁用、未合并任何重要记忆；未修改 Hermes/OpenClaw 本地 MEMORY/USER 或 skills。

## 健康状态

- `shared-memory-client health`：ok=true
- 版本：1.3.0
- memories：77
- embeddings：74
- local_tasks：39
- DB：`/opt/shared-agent-memory/data/memory.sqlite`
- Vault：`/root/obsidian-vault/shared-memory`
- 模型状态：classifier=null；embedding=`nomic-embed-text`；local_worker=null

## Layers 统计

| kind | count | active | expired |
|---|---:|---:|---:|
| success | 26 | 25 | 0 |
| service_state | 12 | 11 | 0 |
| decision | 11 | 5 | 0 |
| fact | 11 | 10 | 0 |
| temporary | 8 | 0 | 0 |
| failure | 3 | 2 | 0 |
| preference | 3 | 1 | 0 |
| noise | 2 | 1 | 0 |
| todo | 1 | 1 | 0 |

## 维护动作结果

- `expire`：ok=true，disabled=0
- `export`：ok=true，导出路径 `/root/obsidian-vault/shared-memory/exports/shared-memory.md`
- `reindex`：ok=true，export 完成，embedded=0，failed=0
- `embed --limit 200`：ok=true，model=`nomic-embed-text`，embedded=0，failed=0

## 候选问题（仅报告，不自动处理）

1. **调度规则存在上下文差异候选**：debug context 中仍有高重要度记忆称 2026-06-23 用户删除“避免 BJT 00:00-07:00 创建任务”的旧规则，并允许 03:00 skills 整理；而当前共享记忆维护技能说明要求维护轮转尊重 BJT 00:00-07:00 避免窗口。本巡检不修改该决策，建议人工确认是否需要新增更精确的“仅共享记忆维护任务避开 00:00-07:00”的限定说明。
2. **千问/qwen 历史引用候选**：debug context 中确认当前状态为“不再使用千问/qwen，Ollama 仅保留 nomic-embed-text 用于 embeddings”；但技能说明中仍描述 `add-auto`/分类使用 qwen2.5 的历史架构。当前 health 显示 classifier=null，与“已移除 qwen”一致；建议后续人工确认是否更新技能文档表述，避免误读。
3. **重复学习类 success 候选**：2026-06-24/25/26 每日开源学习存在多条相近 success 记录，可能可压缩为索引型摘要；本次按边界仅记录候选，不自动合并或禁用。

## 是否需要人工确认

- 需要人工确认：上面 3 类候选如需治理，需用户明确授权后再禁用/合并/改写。
- 无紧急异常：健康检查正常，过期处理未禁用条目，导出/重建索引/嵌入均成功。