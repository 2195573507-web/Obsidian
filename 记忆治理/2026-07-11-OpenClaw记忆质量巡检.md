---
title: "OpenClaw记忆质量巡检 - 2026-07-11"
date: 2026-07-11
updated: 2026-07-11
type: report
category: "记忆治理"
source: "每日任务"
status: "待确认清单"
---

# OpenClaw记忆质量巡检 — 2026-07-11

## 概览

| 指标 | 数值 |
|------|------|
| 审查范围 | shared-memory、OpenClaw memory、Obsidian 共享记忆导出、现有治理报告 |
| 发现问题数 | 9 |
| 需要用户确认 | 4 |
| 建议动作 | keep / disable / expire / merge / rewrite |
| 自动删除 | 0 |

## 结论摘要

本轮未执行任何删除、禁用或重写操作。已识别的主要问题集中在两类：

1. 已知过期或已变更背景仍被保留为可能当前事实，容易误导后续任务。
2. Obsidian 共享导出里存在一批低信息量或缺少正式描述的 skill 目录，适合进入人工确认或后续整理队列，但不应直接清理。

## 发现的问题

### 1. 已知过期主题：BJT 03:00-05:00 自动学习任务

- 严重级别：高
- 证据来源：既有治理报告主题约束；本次检索未发现新的有效支撑
- 现状判断：属于历史任务约束，若继续作为当前规则会误导调度和排期
- 建议动作：`expire`
- 备注：需要用户确认后再决定是否从当前知识路径中降权或改写为历史说明

### 2. 已知过期主题：local-first 作为全局前置路由

- 严重级别：高
- 证据来源：历史治理提示；当前环境说明与既有报告已指向非 qwen-first / 非 local-first 的现行状态
- 现状判断：保留为当前前置路由会造成错误默认认知
- 建议动作：`rewrite`
- 备注：建议改写为“历史配置/曾用策略”，并注明不再是全局前置路由

### 3. shared-agent-memory 服务存在双实例进程

- 严重级别：中
- 证据来源：进程检查
- 现状判断：同类服务同时存在两个 uvicorn 进程，端口分别为 19400 与 9400；目前无法仅凭进程表判断是否为主备、旧实例或重复部署
- 建议动作：`investigate`
- 备注：需要结合服务配置、健康探针或启动脚本进一步确认

### 4. embedding 进程存在，说明向量能力在线

- 严重级别：低
- 证据来源：进程检查
- 现状判断：ollama embedding 进程在运行，暂未见直接异常
- 建议动作：`keep`
- 备注：本次未验证 embedding 数量或最近增量，只能视为基础健康信号

### 5. local_tasks 与 vector index 状态无法从当前快照直接确认

- 严重级别：中
- 证据来源：本次未找到直连状态输出
- 现状判断：缺少可核验指标，不能判定是否正常
- 建议动作：`investigate`
- 备注：需要后续补充服务自检、目录盘点或仪表盘数据

### 6. Obsidian 共享导出中存在无描述 skill 目录

- 严重级别：中
- 证据来源：技能治理与报告索引
- 涉及对象：`apple`、`email`、`media`、`mlops`、`note-taking`、`productivity`、`research`、`smart-home`、`social-media`、`autonomous-ai-agents`、`data-science`、`devops`、`github`、`software-development`
- 现状判断：其中一部分像分类目录或内容占位，信息密度较低；直接删除风险高
- 建议动作：`rewrite` 或 `keep`
- 备注：建议优先补描述、补入口、合并近义分类，而不是清理

### 7. OpenClaw 与 Hermes 两侧存在同名 `github`

- 严重级别：中
- 证据来源：技能治理报告
- 现状判断：属于跨层同名，不等同于重复错误，但确实会增加检索歧义
- 建议动作：`merge`
- 备注：更适合明确主入口、保留覆盖层说明，避免后续引用混淆

### 8. `qqbot-channel` 存在结构性质量问题

- 严重级别：中
- 证据来源：技能治理报告摘要
- 现状判断：描述过长且存在相对链接失效问题，影响维护质量和检索一致性
- 建议动作：`rewrite`
- 备注：不建议删除，优先修正文档结构与链接

### 9. 旧报告中提及的部分 skill 缺少标准 `SKILL.md`

- 严重级别：低
- 证据来源：技能治理报告
- 现状判断：更像目录型组织，而不是无效记忆；不宜当作噪声直接清除
- 建议动作：`keep`
- 备注：如果后续要做结构收敛，再集中处理

## 待人工确认清单

- BJT 03:00-05:00 自动学习任务是否应永久标记为历史项。
- local-first 作为全局前置路由是否需要正式写入“已废弃”说明。
- shared-agent-memory 的两个 uvicorn 实例是否为预期双实例部署。
- `github` 同名 skill 是否需要统一主入口与说明层级。

## 检索健康

### 已确认

- `shared-agent-memory` 服务：发现两个运行中的 uvicorn 进程，说明服务至少有活动实例，但是否重复部署未定。
- `embedding`：存在 Ollama embedding 进程，基础向量能力在线。

### 未确认

- `embedding 数`：本次未读取统计值。
- `local_tasks`：本次未读取目录或运行态状态。
- `vector index`：本次未读取索引健康信息。

### 结论

当前只能判断“部分基础能力在线”，不能判断“检索链路完全健康”。建议下次巡检补充：服务状态页、索引统计、最近构建时间、失败计数。

## 建议动作汇总

- `keep`：embedding 进程在线、部分目录型 skill 目前不宜清理。
- `disable`：本轮无可直接执行对象，且用户未授权自动禁用。
- `expire`：BJT 03:00-05:00 自动学习任务。
- `merge`：跨层同名 `github` 的入口与说明。
- `rewrite`：local-first 全局前置路由、`qqbot-channel` 结构、低信息量 skill 目录说明。
- `investigate`：shared-agent-memory 双实例、embedding 数、local_tasks、vector index 状态。

## 保存位置

`/root/obsidian-vault/记忆治理/2026-07-11-OpenClaw记忆质量巡检.md`
