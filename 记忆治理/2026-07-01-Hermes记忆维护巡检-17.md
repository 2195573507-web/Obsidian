# Hermes 共享/长期记忆维护巡检（2026-07-01 17:14 BJT）

## 执行范围
- 执行项：`health`、`layers`、`expire`、debug `context`、`export`、`reindex`、`embed --limit 200`。
- 安全边界：未删除、未禁用、未合并记忆；未修改 Hermes/OpenClaw 本地 `MEMORY/USER`；未整理 skills；未写入凭证明文；未创建 cron。

## 健康状态
- 服务状态：正常（`ok: true`）
- 版本：`1.3.0`
- 记忆总数：`81`
- embeddings：`78`
- local_tasks：`39`
- 数据库：`/opt/shared-agent-memory/data/memory.sqlite`
- vault：`/root/obsidian-vault/shared-memory`
- 模型状态：classifier=`null`，embedding=`nomic-embed-text`，local_worker=`null`

## Layers 统计
| kind | count | active | expired |
|---|---:|---:|---:|
| success | 30 | 29 | 0 |
| service_state | 12 | 11 | 0 |
| decision | 11 | 5 | 0 |
| fact | 11 | 10 | 0 |
| temporary | 8 | 0 | 0 |
| failure | 3 | 2 | 0 |
| preference | 3 | 1 | 0 |
| noise | 2 | 0 | 0 |
| todo | 1 | 1 | 0 |

## 维护结果
- `expire`：成功，禁用过期条目 `0` 条。
- `export` / `reindex` / `embed --limit 200`：成功；本轮新增 embedding `0`，失败 `0`。

## 候选问题（仅报告，不自动处理）
1. **调度偏好存在候选冲突**：debug context 中仍有高优先级历史决策称用户已删除“避免 BJT 00:00-07:00 创建任务”的旧规则；但当前共享记忆维护技能的轮转模式仍要求尊重“避免 BJT 00:00-07:00”。建议人工确认当前通用调度准则是否应统一。
2. **qwen/千问相关说明存在候选过期**：服务健康状态与高优先级决策均显示 classifier/local_worker 为 `null`、已移除 qwen，仅保留 `nomic-embed-text`；但技能说明中仍有 `add-auto` 使用 qwen2.5 的描述。建议后续人工统一文档/技能描述，避免误读。
3. **每日开源学习类 success 条目较多**：特别是 2026-06-26 有多条相近总结，暂不自动合并；可作为候选去重/归纳对象。

## 是否需要人工确认
- 需要人工确认：是（主要是调度规则与 qwen 说明的候选冲突/过期）。
- 不需要紧急处理：服务健康、导出、索引、embedding 均正常。
