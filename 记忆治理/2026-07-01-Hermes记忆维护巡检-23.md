# Hermes 共享/长期记忆维护巡检

- 巡检时间（BJT）：2026-07-01 23:13:22 CST +0800
- 健康状态：正常（`ok=true`，版本 `1.3.0`）
- 数据库：`/opt/shared-agent-memory/data/memory.sqlite`
- Vault：`/root/obsidian-vault/shared-memory`
- 记忆总数：81；embeddings：78；local_tasks：39
- 模型状态：classifier=`null`，embedding=`nomic-embed-text`，local_worker=`null`

## Layers 统计

| kind | count | active | expired |
|---|---:|---:|---:|
| success | 30 | 29 | 0 |
| service_state | 12 | 11 | 0 |
| decision | 11 | 5 | 0 |
| fact | 11 | 10 | 0 |
| temporary | 8 | 0 | 0 |
| failure | 3 | 2 | 0 |
| preference | 3 | 1 | 0 |
| noise | 2 | 0 | 0 |
| todo | 1 | 1 | 0 |

## 本次维护动作

- `shared-memory-client expire`：完成，disabled=0。
- `shared-memory-client export`：完成，导出到 `/root/obsidian-vault/shared-memory/exports/shared-memory.md`。
- `shared-memory-client reindex`：已执行。
- `shared-memory-client embed --limit 200`：完成，embedded=0，failed=0。

## 候选问题（仅报告，不自动处理）

1. **调度规则候选冲突**：debug context 中存在 2026-06-23 高重要度决策，称用户已废弃“避免北京时间 00:00-07:00 创建任务”的旧规则；但当前维护 skill 文档仍包含“避免 BJT 00:00-07:00”的通用约束。建议人工确认后统一文档/记忆表述。
2. **qwen/千问状态候选陈旧**：debug context 与 health 均显示当前不使用 qwen/千问，classifier/local_worker 为 null，仅保留 `nomic-embed-text`；但 skill 文档仍描述 `add-auto` 使用 `qwen2.5:3b` 分类。建议人工确认后更新文档或保留为历史说明。
3. **学习类 success 条目较多**：多条每日开源学习结论重复覆盖相近主题，可作为后续人工合并候选；本次未自动合并、禁用或删除。

## 是否需要人工确认

需要人工确认：上述 3 类均属于候选治理项，按任务边界本次只生成报告，不做删除、禁用、合并或本地 MEMORY/USER 修改。
