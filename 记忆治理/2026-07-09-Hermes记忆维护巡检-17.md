# Hermes 共享记忆维护巡检（2026-07-09 17:01 BJT）

## 执行范围

- 执行任务：health、layers、expire、debug context、export、reindex、embed --limit 200。
- 安全边界：未删除记忆，未禁用重要记忆，未合并用户偏好，未修改 Hermes/OpenClaw 本地 MEMORY/USER，未整理 skills，未写入凭证明文。

## 健康状态

- 服务状态：ok=true。
- 版本：1.3.0。
- SQLite：/opt/shared-agent-memory/data/memory.sqlite。
- Vault：/root/obsidian-vault/shared-memory。
- 记忆总数：95。
- embeddings：92。
- local_tasks：39。
- 模型状态：classifier=null，embedding=nomic-embed-text，local_worker=null。

## Layers 统计

| kind | count | active | expired |
|---|---:|---:|---:|
| success | 41 | 40 | 0 |
| service_state | 15 | 14 | 0 |
| decision | 11 | 5 | 0 |
| fact | 11 | 10 | 0 |
| temporary | 8 | 0 | 0 |
| failure | 3 | 2 | 0 |
| preference | 3 | 1 | 0 |
| noise | 2 | 0 | 0 |
| todo | 1 | 1 | 0 |

## 本次维护结果

- expire：ok=true，disabled=0。
- export：已更新 /root/obsidian-vault/shared-memory/exports/shared-memory.md。
- reindex：命令已执行，未返回错误。
- embed --limit 200：ok=true，model=nomic-embed-text，embedded=0，failed=0。

## 候选问题

1. 调度规则候选冲突：debug context 中存在“用户 2026-06-23 删除夜间禁做规则”的高重要度 decision；而当前巡检任务说明仍要求遵守 BJT 09:00-23:00 轮转并避免 00:00-07:00。由于本任务明确给出当前边界，本次不处理旧记忆；建议人工确认是否需要把二者整理为“通用规则已废弃，但共享记忆维护巡检仍按专用 schedule 执行”。
2. qwen 状态候选重复/历史化：debug context 显示高重要度 decision/service_state 均指向“当前不再使用 qwen，classifier=null，仅保留 nomic-embed-text”；技能说明中仍保留 add-auto/qwen 相关历史说明。运行健康状态与该 decision 一致，本次不修改技能或记忆；建议人工确认是否需要添加一条更精简的当前状态索引，或标记旧 qwen workflow 为历史参考。
3. daily open-source learning 相关 success 记忆数量较多，debug context 中占用较长输出，部分日期存在多条相近总结。它们不是错误，但可能影响召回密度；建议人工确认是否需要做候选合并报告。

## 是否需要人工确认

- 需要人工确认：仅当要处理上述候选冲突/重复时需要。
- 无须立即人工处理：服务健康、导出和索引维护均正常，本次未发现需要自动干预的异常。
