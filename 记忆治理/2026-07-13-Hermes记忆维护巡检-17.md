# Hermes 共享/长期记忆维护巡检

- 巡检时间：2026-07-13 17:00:26 BJT
- 健康状态：正常；shared-memory-client health 返回 ok=true，版本 1.3.0。
- 数据状态：memories=103，embeddings=100，local_tasks=39，数据库 /opt/shared-agent-memory/data/memory.sqlite。
- 分层数量：success 48/active 47；service_state 15/active 14；fact 12/active 11；decision 11/active 5；temporary 8/active 0；failure 3/active 2；preference 3/active 1；noise 2/active 0；todo 1/active 1。
- 过期处理：shared-memory-client expire 成功，disabled=0。
- 维护动作：export 成功；reindex 成功且 embedded=0 failed=0；embed --limit 200 成功且 embedded=0 failed=0。

## 候选问题

1. 时间策略存在候选冲突：当前任务指令要求尊重 BJT 09:00-23:00 轮转并避免 00:00-07:00；debug 召回中存在 2026-06-23 的高重要度决策，说明旧的“夜间禁做/避免 00:00-07:00”规则已废弃。由于本次 cron 明确给出时间边界，已按本任务指令执行，仅记录候选，不自动修改记忆。
2. ESP 服务器召回索引存在内容相近的高重要度 service_state 条目，可能属于重复强化索引。当前未处理，建议人工确认是否需要保留双索引或合并为一条。
3. 每日开源学习与历史导入类 success/fact 条目数量较多，但未发现本轮必须自动处理的问题。按安全边界不自动合并或降噪。

## 是否需要人工确认

需要人工确认：仅当用户希望清理策略冲突或重复 ESP 召回索引时再处理。否则本轮无须人工处理。
