# Hermes 共享/长期记忆维护巡检（2026-07-13 19:00 BJT）

- 巡检时间：2026-07-13 19:00:36 CST (+0800)
- 健康状态：OK；shared-memory-client 版本 1.3.0；数据库 `/opt/shared-agent-memory/data/memory.sqlite`；导出目录 `/root/obsidian-vault/shared-memory`。
- 总量：memories 103；events 0；embeddings 100；local_tasks 39。
- 模型状态：classifier 为 null（符合已移除本地千问后的状态）；embedding 使用 `nomic-embed-text`；local_worker 为 null。
- layers：success 48/active 47；service_state 15/active 14；fact 12/active 11；decision 11/active 5；temporary 8/active 0；failure 3/active 2；preference 3/active 1；noise 2/active 0；todo 1/active 1。
- expire：disabled 0，无本轮自动过期禁用。
- 维护动作：已执行 export、reindex、embed --limit 200；embed 结果 embedded 0、failed 0。

## 候选问题

1. `context --mode debug` 中仍可见较多历史千问/qwen、本地任务池相关说明；但高优先级当前决策已明确“服务器现在没有使用千问 / 已移除本地千问”，本轮未自动禁用或合并旧记忆。
2. skills/cron 时间策略相关记忆存在历史变更痕迹：有“删除夜间禁做规则”的旧决策；本次任务自身明确运行于 BJT 09:00-23:00 且不要创建新 cron，本轮不做处理。
3. success 类条目数量偏多，包含每日开源学习记录；当前未发现必须立即人工处理的健康异常，但后续可人工确认是否需要候选整理/降噪。

## 是否需要人工确认

- 当前健康状态正常，无须立即人工处理。
- 如要处理候选重复/历史策略条目，需要用户明确确认后再禁用、合并或重写摘要；本轮仅报告，不修改重要记忆。