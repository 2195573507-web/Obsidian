# Hermes 共享/长期记忆维护巡检

- 巡检时间（BJT）：2026-07-10 23:06:41 CST +0800
- 任务来源：Hermes 定时巡检（共享/长期记忆维护）
- 执行边界：仅健康检查、layers 统计、expire、context debug、export、reindex、embed、生成候选报告；未删除、禁用、合并任何重要记忆；未修改 Hermes/OpenClaw 本地 MEMORY/USER；未整理 skills；未写入凭证明文。

## 健康状态

- `shared-memory-client health`：ok=true
- 版本：1.3.0
- memories：96
- embeddings：93
- local_tasks：39
- db：`/opt/shared-agent-memory/data/memory.sqlite`
- vault：`/root/obsidian-vault/shared-memory`
- models：classifier=null；embedding=`nomic-embed-text`；local_worker=null

## Layers 统计

| kind | count | active | expired |
|---|---:|---:|---:|
| success | 42 | 41 | 0 |
| service_state | 15 | 14 | 0 |
| decision | 11 | 5 | 0 |
| fact | 11 | 10 | 0 |
| temporary | 8 | 0 | 0 |
| failure | 3 | 2 | 0 |
| preference | 3 | 1 | 0 |
| noise | 2 | 0 | 0 |
| todo | 1 | 1 | 0 |

## 本次维护命令结果

- `expire`：ok=true，disabled=0
- `export`：完成，导出路径 `/root/obsidian-vault/shared-memory/exports/shared-memory.md`
- `reindex`：ok=true，embedded=0，failed=0
- `embed --limit 200`：ok=true，model=`nomic-embed-text`，embedded=0，failed=0

## 候选问题

1. 排程/夜间规则候选冲突：debug context 召回到旧/新规则并存。一条记录说明“删除/废弃晚上不允许做任务、避免 00:00-07:00 创建任务的旧规则”，本次巡检任务说明又要求尊重当前任务的 BJT 09:00-23:00 运行窗口并避免额外创建 cron。建议人工确认是否需要为“共享记忆巡检任务”单独保留更高优先级的排程边界说明。
2. qwen 状态候选澄清：debug context 显示当前服务器不再使用 qwen/qwen2.5，且 shared-agent-memory classifier/local_worker 为 null；同时 embeddings 仍使用 `nomic-embed-text`。这与当前状态一致，未发现需要处理的问题。
3. ESP 服务器召回重复候选：debug context 同时召回两条高重要度 ESP service_state 索引，内容高度重叠但第二条更完整。建议后续人工确认是否禁用或降权较短索引，避免召回冗余；本次未处理。

## 是否需要人工确认

- 需要人工确认：仅当要处理 ESP 重复召回、或要固化“共享记忆巡检任务”的独立排程边界时。
- 无需立即人工处理：健康状态正常，过期处理无禁用项，导出/索引/嵌入均成功，未发现运行异常。
