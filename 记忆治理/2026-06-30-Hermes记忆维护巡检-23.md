# Hermes 共享/长期记忆维护巡检（2026-06-30 23:00 BJT）

- 巡检时间：2026-06-30 23:00:50 CST
- 健康状态：正常（ok=true），shared-memory version 1.3.0
- 数据库：`/opt/shared-agent-memory/data/memory.sqlite`
- 导出文件：`/root/obsidian-vault/shared-memory/exports/shared-memory.md`
- 总记忆数：80；embeddings：77；local_tasks：39
- 模型状态：classifier=null；embedding=`nomic-embed-text`；local_worker=null

## Layers 统计

| kind | count | active | expired |
|---|---:|---:|---:|
| success | 29 | 28 | 0 |
| service_state | 12 | 11 | 0 |
| decision | 11 | 5 | 0 |
| fact | 11 | 10 | 0 |
| temporary | 8 | 0 | 0 |
| failure | 3 | 2 | 0 |
| preference | 3 | 1 | 0 |
| noise | 2 | 0 | 0 |
| todo | 1 | 1 | 0 |

## 本次维护动作

- `health`：通过。
- `layers`：通过。
- `expire`：通过，disabled=0。
- `context --mode debug`：已检查模型路由、server service_state、cron、skills、memory maintenance 相关上下文。
- `export`：通过，导出路径为 `/root/obsidian-vault/shared-memory/exports/shared-memory.md`。
- `reindex`：通过，embedded=0，failed=0。
- `embed --limit 200`：通过，embedded=0，failed=0。

## 候选问题（仅报告，未自动处理）

1. **调度偏好存在上下文差异候选**：debug context 中仍有高重要度决策表示“删除/废弃晚上不允许做任务、避免 BJT 00:00-07:00 创建任务的旧规则”；而当前共享记忆维护任务说明要求遵守本任务的 BJT 09:00-23:00 轮转，不新建 cron。建议人工确认该差异是否仅限不同任务域。
2. **qwen/千问相关说明可能有历史残留**：当前健康状态显示 `classifier=null`、`local_worker=null`，且 debug context 有高重要度决策说明已移除 qwen2.5，仅保留 `nomic-embed-text`；但部分技能说明仍描述 add-auto 使用 qwen2.5 分类。建议后续人工确认文档/技能说明是否需要更新为“历史配置”。
3. **学习类 success 记忆较多**：每日开源学习 success 条目数量较高，未发现需要自动禁用的明确冲突；如用户希望压缩，可人工确认后做合并候选。

## 是否需要人工确认

- 需要人工确认：是（仅针对上述候选差异/可能过时说明）。
- 本次未删除、未禁用、未合并任何重要记忆，未修改 Hermes/OpenClaw 本地 MEMORY/USER，未整理 skills，未写入凭证明文。
