# Hermes 记忆维护巡检候选报告

- 巡检时间：2026-07-12 19:01 CST（BJT）
- 报告类型：候选报告
- 执行边界：仅检查、导出、重建与记录，不自动删除、禁用或合并任何记忆

## 健康状态

- `shared-memory-client health` 返回 `ok=true`
- 版本：`1.3.0`
- 记忆总数：`103`
- 事件数：`0`
- 向量数：`100`
- 本地任务数：`39`
- 数据库：`/opt/shared-agent-memory/data/memory.sqlite`
- 导出目录：`/root/obsidian-vault/shared-memory`

## 分层统计

- `success`：48 总，47 active，0 expired
- `service_state`：15 总，14 active，0 expired
- `fact`：12 总，11 active，0 expired
- `decision`：11 总，5 active，0 expired
- `temporary`：8 总，0 active，0 expired
- `failure`：3 总，2 active，0 expired
- `preference`：3 总，1 active，0 expired
- `noise`：2 总，0 active，0 expired
- `todo`：1 总，1 active，0 expired

## 候选问题

- `context --mode debug` 中仍可见一条较旧的 cron 时间规则记忆，描述为“北京时间 00:00-07:00 可安排任务”，与更近的决策类记忆存在语义冲突，建议人工确认是否需要保留为历史记录。
- 现有 `decision` 层 active 数只有 `5/11`，说明较多决策已被压低或转为非 active；如后续检索命中率下降，可人工复核是否存在过时决策占位。
- `preference` 层 active 仅 `1/3`，目前看是正常收敛，但如果用户近期确实更新了长期偏好，建议确认是否有遗漏的正式记忆条目。

## 本次动作结果

- `expire`：`disabled=0`
- `export`：成功更新 `/root/obsidian-vault/shared-memory/exports/shared-memory.md`
- `reindex`：成功
- `embed --limit 200`：`embedded=0`，`failed=0`

## 是否需要人工确认

- 需要人工确认：是
- 原因：存在一条时间规则的历史/当前语义冲突候选，适合人工判断是否仅保留历史说明或补写更明确的决策记忆。
