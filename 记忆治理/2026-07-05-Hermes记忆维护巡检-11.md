# Hermes 共享/长期记忆维护巡检（2026-07-05 11:00 BJT）

- 巡检时间：2026-07-05 11:00:49 CST（BJT）
- 执行边界：仅健康检查、layers、expire、debug context、export、reindex、embed 与候选报告；未删除、未禁用、未合并记忆，未修改 Hermes/OpenClaw 本地 MEMORY/USER，未整理 skills。

## 健康状态

```json
{
  "ok": true,
  "version": "1.3.0",
  "memories": 84,
  "events": 0,
  "embeddings": 81,
  "local_tasks": 39,
  "db": "/opt/shared-agent-memory/data/memory.sqlite",
  "vault": "/root/obsidian-vault/shared-memory",
  "models": {
    "classifier": null,
    "embedding": "nomic-embed-text",
    "local_worker": null
  }
}
```

## Layers 统计

| kind | count | active | expired |
|---|---:|---:|---:|
| success | 33 | 32 | 0 |
| service_state | 12 | 11 | 0 |
| decision | 11 | 5 | 0 |
| fact | 11 | 10 | 0 |
| temporary | 8 | 0 | 0 |
| failure | 3 | 2 | 0 |
| preference | 3 | 1 | 0 |
| noise | 2 | 0 | 0 |
| todo | 1 | 1 | 0 |

## 本次维护动作结果

- `expire`：ok，disabled=0。
- `export`：ok，导出路径 `/root/obsidian-vault/shared-memory/exports/shared-memory.md`。
- `reindex`：ok，embedded=0，failed=0。
- `embed --limit 200`：ok，model=`nomic-embed-text`，embedded=0，failed=0。

## 候选问题（仅报告，不自动处理）

1. **模型路由/千问状态候选冲突**：debug context 中当前高优先级记忆显示 2026-06-23/24 已明确“不再使用千问/qwen”，health 也显示 `classifier=null`、`local_worker=null`、embedding 仍为 `nomic-embed-text`。但部分共享记忆 skill 文本仍描述 `add-auto` 使用 qwen2.5 分类；建议人工确认是否需要更新相关说明或保留为历史说明。
2. **时间规则候选冲突**：debug context 中存在“删除夜间禁做规则、03:00 可执行”的高优先级决策；本次巡检指令仍要求记忆维护遵守 BJT 09:00-23:00 轮换且不要新建 cron。建议维持本巡检任务的显式边界，若要统一全局时间策略需人工确认。
3. **重复学习类 success 候选**：`success` 层数量较多（33/32 active），debug context 中同日期开源学习摘要存在多条相近记录；不影响健康，但后续可由人工确认是否合并/降噪。
4. **OpenClaw skill_workshop 待办仍未解决**：todo 中仍有 pending skill proposals 需支持的审批路径；本任务不绕过审批、不安装 skills。

## 是否需要人工确认

- 系统健康：正常。
- 本次无自动处置项。
- 需要人工确认：上述候选问题如需清理、禁用、合并或改写记忆/skill 说明时再处理。
