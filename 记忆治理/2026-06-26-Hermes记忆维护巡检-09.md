# Hermes 共享/长期记忆维护巡检（2026-06-26 09:00 BJT）

- 巡检时间：2026-06-26 09:00:17 CST（BJT）
- 健康状态：`shared-memory-client` 初次连接 `127.0.0.1:9400` 失败（Connection refused）；已启动 `shared-agent-memory.service` 后复检通过。
- 服务状态：`shared-agent-memory.service` 当前运行于 `0.0.0.0:9400`；另发现一个既有 uvicorn 进程监听 `0.0.0.0:19400`，需人工确认是否为预期的并行实例/旧实例。
- 数据概况：health ok，版本 1.3.0；memories=58，events=0，embeddings=54，local_tasks=37；DB `/opt/shared-agent-memory/data/memory.sqlite`。
- layers：service_state 12/active 11；decision 10/active 4；fact 10/active 9；success 9/active 8；temporary 8/active 0；failure 3/active 2；preference 3/active 1；noise 2/active 1；todo 1/active 1。
- expire：disabled=0。
- export/reindex/embed：均执行成功；export `/root/obsidian-vault/shared-memory/exports/shared-memory.md`；reindex embeddings embedded=0 failed=0；embed --limit 200 embedded=0 failed=0。

## 候选问题

1. **服务可用性异常（需要人工确认）**：本轮开始时 9400 端口拒绝连接，`shared-agent-memory.service` 处于 inactive/dead；巡检期间已启动恢复。建议确认为什么 2026-06-25 16:34 UTC 被停止，以及是否应保持 enable/常驻。
2. **端口/实例漂移（需要人工确认）**：同时存在 9400 与 19400 两个 shared-memory 相关 uvicorn 实例；CLI 固定指向 9400，19400 返回健康正常但不在当前 skill 文档的 API 端口内。建议确认是否为迁移残留。
3. **记忆候选冲突（仅报告，不处理）**：debug context 中仍召回“夜间禁做规则已废弃”的高优先级历史决策；本巡检任务自身要求 BJT 09:00-23:00 运行、不要新建 cron。当前未自动修改或禁用，建议只在后续治理时确认该规则的适用范围（通用任务 vs 共享记忆维护任务）。

## 是否需要人工确认

需要。主要是确认 `shared-agent-memory.service` 被停止原因、9400/19400 双实例是否合理，以及是否需要将 9400 服务设置为预期的长期启用状态。本次未删除、禁用、合并任何重要记忆，未修改 Hermes/OpenClaw 本地 MEMORY/USER，未整理 skills，未写入凭据。
