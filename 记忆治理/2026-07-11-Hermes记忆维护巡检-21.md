# Hermes 记忆维护巡检

- 时间：2026-07-11 21:00 BJT
- 健康状态：`ok=true`，版本 `1.3.0`
- 记忆总量：100
- 向量总量：97
- 本地任务数：39

## 各层数量

- success：45，总计 44 active，0 expired
- service_state：15，总计 14 active，0 expired
- fact：12，总计 11 active，0 expired
- decision：11，总计 5 active，0 expired
- temporary：8，总计 0 active，0 expired
- failure：3，总计 2 active，0 expired
- preference：3，总计 1 active，0 expired
- noise：2，总计 0 active，0 expired
- todo：1，总计 1 active，0 expired

## 候选问题

- debug 上下文中仍可见一批较高重要度的历史 imported decision / service_state 记忆，内容集中在 ESP、qwen 移除、技能整理定时、写入权限与 Hermes 监控等主题；本次未发现明显冲突，但存在较多旧状态回显，后续如要做记忆清理应先人工确认。
- `temporary` 层当前有 8 条，`noise` 层有 2 条，均未激活；属于可继续观察的低优先级存量，不建议自动处置。

## 处理结果

- 已执行 `expire`，结果：`disabled=0`
- 已执行 `export`
- 已执行 `reindex`
- 已执行 `embed --limit 200`
- 需要人工确认：否

## 备注

- 本次巡检未进行删除、禁用、合并或修改本地 `MEMORY/USER`。
- 共享导出文件已更新到 `/root/obsidian-vault/shared-memory/exports/shared-memory.md`
