# Hermes 记忆维护巡检

- 时间：2026-06-26 19:02:33 CST（BJT）
- 健康：`ok=true`，版本 `1.3.0`；memories `64`，embeddings `61`，local_tasks `39`
- 层统计：success `15/14`，service_state `12/11`，decision `10/4`，fact `10/9`，temporary `8/0`，failure `3/2`，preference `3/1`，noise `2/1`，todo `1/1`
- 过期处理：`expire` 完成，disabled `0`
- 候选问题：debug 回查命中多条历史决策，主要是“已移除 qwen/旧时间规则/旧迁移说明”类高优先级条目；当前属于已知历史上下文，未发现需要自动处理的新增冲突
- 处理结论：已完成 `export`、`reindex`、`embed --limit 200`；本次无须人工处理
- 报告路径：`/root/obsidian-vault/shared-memory/exports/shared-memory.md`
