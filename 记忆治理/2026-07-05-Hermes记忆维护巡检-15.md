# Hermes 共享/长期记忆维护巡检

- 巡检时间：2026-07-05 15:00:19 CST（BJT）
- 执行范围：health、layers、expire、debug context、export、reindex、embed；未删除、未禁用、未合并重要记忆，未修改 Hermes/OpenClaw 本地 MEMORY/USER，未整理 skills。

## 健康状态

- `shared-memory-client health`：ok=true
- 版本：1.3.0
- memories：84
- embeddings：81
- local_tasks：39
- DB：`/opt/shared-agent-memory/data/memory.sqlite`
- Vault：`/root/obsidian-vault/shared-memory`
- 模型状态：classifier=null；embedding=`nomic-embed-text`；local_worker=null

## Layers 统计

| kind | count | active | expired |
|---|---:|---:|---:|
| success | 33 | 32 | 0 |
| service_state | 12 | 11 | 0 |
| decision | 11 | 5 | 0 |
| fact | 11 | 10 | 0 |
| temporary | 8 | 0 | 0 |
| failure | 3 | 2 | 0 |
| preference | 3 | 1 | 0 |
| noise | 2 | 0 | 0 |
| todo | 1 | 1 | 0 |

## 维护动作结果

- `expire`：ok=true，disabled=0
- `export`：已执行，输出文件为 `/root/obsidian-vault/shared-memory/exports/shared-memory.md`
- `reindex`：ok=true，embedded=0，failed=0
- `embed --limit 200`：ok=true，model=`nomic-embed-text`，embedded=0，failed=0

## 候选问题

1. **时间规则候选冲突**：debug context 中仍有高重要度 decision 表示“删除/废弃晚上不允许做任务、避免 BJT 00:00-07:00 创建任务”的旧规则；但当前 shared-agent-memory 维护说明仍要求维护调度尊重 BJT、避免 00:00-07:00。建议人工确认这两条规则的适用范围：是仅针对本维护轮换保留避开凌晨，还是全局已废弃。
2. **qwen/千问状态候选重复或文档不一致**：debug context 与 health 均显示当前不使用 qwen，classifier/local_worker 为 null，仅保留 `nomic-embed-text`；但技能说明中仍包含 `add-auto` 使用本地 Ollama `qwen2.5:3b` 的描述。建议人工确认是否需要更新技能文档或保留为历史说明。
3. **OpenClaw skill proposal 待办仍存在**：仍有 todo 指向 5 个 pending proposals，需支持审批路径后人工处理；本次未绕过审批、未安装 skills。
4. **学习类 success 数量较多**：多条每日开源学习记录仍活跃，可能存在归档/合并候选；按边界仅报告，不自动合并。

## 是否需要人工确认

- 需要人工确认：时间规则适用范围、qwen 技能说明是否更新、OpenClaw pending skill proposals 后续处理、学习类 success 是否需要人工治理。
- 本次无健康异常，无过期禁用动作，无 embedding 失败。
