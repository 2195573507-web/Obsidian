---
title: "OpenClaw记忆质量巡检 - 2026-07-12"
date: 2026-07-12
updated: 2026-07-12
type: report
category: "记忆治理"
source: "每日任务"
status: "待确认清单"
auto_delete: false
---

# OpenClaw记忆质量巡检 - 2026-07-12

## 概览

| 指标 | 数值 |
|------|------|
| 审查范围 | shared-memory、OpenClaw memory、Obsidian 共享记忆导出、既有治理报告 |
| 发现问题数 | 10 |
| 需要用户确认 | 5 |
| 自动删除/禁用 | 0 |
| 建议动作集合 | keep / disable / expire / merge / rewrite / investigate |

## 执行原则

本轮只审计，不删除、不禁用、不改写任何记忆。报告中仅记录摘要、路径、id 与建议动作；不展开敏感内容原文。

## 检索健康

### shared-agent-memory

- 状态：`active`，健康接口正常返回。
- 开机自启：`disabled`。当前服务在线，但重启后是否自动恢复需要确认。
- 健康统计：`memories=103`，`embeddings=100`，`local_tasks=39`。
- 数据库：`/opt/shared-agent-memory/data/memory.sqlite`。
- 模型状态：classifier 为 `null`，embedding 为 `nomic-embed-text`，local_worker 为 `null`，符合“禁用千问/本地分类器、保留 embedding”的既有方向。
- 建议动作：`keep` + `investigate`。保持当前在线状态；确认 disabled 是否为预期。

### shared-memory 数据库统计

- memories：103。
- memory_embeddings：100。
- local_tasks：39。
- local_tasks 状态：`done=14`，`escalate=14`，`fallback=10`，`failed=1`。
- 记忆类型分布中仍有 disabled 的 `temporary`、`noise`、`failure`、`decision` 等历史/低价值类别，但未自动处理。
- 建议动作：`investigate` failed local_task；对 disabled 噪声项保持不动或后续人工确认归档策略。

### OpenClaw memory 索引

- `/root/.openclaw/memory/main.sqlite`：files=6，chunks=24，embedding_cache=56，vector rowids=24。
- meta 显示 provider 为 `ollama`，model 为 `nomic-embed-text`，vectorDims=768。
- 判断：OpenClaw memory 的向量索引基本健康，chunk 与 vector rowid 数量一致。
- 建议动作：`keep`。

### OpenClaw agent memory index

- `/root/.openclaw/agents/main/agent/openclaw-agent.sqlite`：sources=24，chunks=97。
- meta 显示 provider 为 `none`，model 为 `fts-only`。
- 判断：该索引当前是 FTS-only，不是向量索引异常；但若预期启用 agent 级 embedding，需要另行确认。
- 建议动作：`keep` 或 `investigate`，取决于预期配置。

### Wiki lint

- wiki_lint：10 个 warning，0 error，0 contradictions，1 open question，9 provenance gaps。
- 判断：未发现结构性错误或明确矛盾，但存在来源证明不足的问题。
- 建议动作：`rewrite`，后续补来源或降低没有证据的断言权重。

## 发现的问题

### 1. 已知过期主题：旧 BJT 03:00-05:00 自动学习任务

- 严重级别：高。
- 涉及路径/id：`memory/archive/2026-06-23.md#L1-L9`；备份副本 `memory/backups/openclaw-session-memory-compact-20260712T030000Z/2026-06-23.md#L1-L9`；共享记忆 id `f63df781-1af1-461f-90a2-81938d70b525`。
- 判断：检索命中的是“夜间限制废弃 + skills maintenance at BJT 03:00”的相关记录；未命中精确的“03:00-05:00 自动学习任务”当前有效记录。由于用户明确提示旧自动学习任务已过期，相关记忆如果被理解为当前排期规则，可能误导后续任务。
- 建议动作：`expire` 或 `rewrite`。
- 需要用户确认：是。建议将“旧自动学习任务/03:00-05:00 窗口”标为历史项，同时保留“用户允许夜间任务”这一当前偏好。

### 2. local-first 作为全局前置路由的废弃状态需要保持清晰

- 严重级别：高。
- 涉及 id：`995e0b1a-b147-4778-9b16-55d40801e3c8`、`ef9adf19-874c-4a4b-9b01-0f28e06bf0b1`、`58c86f87-4c8a-4b4c-a51c-e4b74d1d3c52`、`54c31b65-b94e-4e65-af53-e81b785e5419`。
- 判断：旧 local-first/qwen 路由相关决策多数已 disabled；`qwen/千问 server usage is historical only` 仍启用，内容方向是历史化说明，建议保留。风险在于备份和归档仍会被搜索命中，可能产生“旧策略仍当前有效”的误读。
- 建议动作：`keep` 已 disabled 的历史记录；对可检索摘要 `rewrite`，强调“非当前全局前置路由”。
- 需要用户确认：是。

### 3. OpenClaw memory 的备份目录被检索命中，造成重复结果

- 严重级别：中。
- 涉及路径：`memory/archive/*` 与 `memory/backups/openclaw-session-memory-compact-*/*`。
- 证据摘要：BJT 03:00 与 local-first 检索均出现 archive 与 backups 副本同时命中。
- 判断：备份内容作为审计来源有价值，但如果进入普通语义检索排序，会放大旧事实和重复事实。
- 建议动作：`merge` 或 `disable` 检索层面的备份目录索引；不要删除备份文件。
- 需要用户确认：是。建议确认是否将 `memory/backups/` 从常规召回中降权或排除。

### 4. shared-memory 中存在已 disabled 的 low-value 类型项

- 严重级别：中。
- 统计摘要：disabled `temporary=8`、`noise=2`、`failure=1`、`preference=2`、`decision=6` 等。
- 判断：这些项当前已 disabled，不应再强影响召回；但它们仍占据数据库与治理关注面。
- 建议动作：`keep` 当前 disabled 状态；后续人工确认是否统一 `expire` 或迁移到归档说明。
- 需要用户确认：否，除非要清理/归档。

### 5. local_tasks 有 1 个 failed 任务

- 严重级别：低。
- 涉及 id：`7dcab30a-0108-4b49-946c-148c1846faa2`。
- 摘要：`report-draft` 任务失败，route 为历史 qwen 路径，错误摘要为超时。
- 判断：更像历史失败任务，当前 classifier/local_worker 已禁用，不代表今天服务异常。
- 建议动作：`expire` 或 `rewrite` 为历史失败记录。
- 需要用户确认：否。

### 6. shared-agent-memory 服务 active 但 disabled

- 严重级别：中。
- 判断：服务当前可用，但重启后未必自动启动。若这是故意避免自启动，则保持；若不是，可能影响记忆检索连续性。
- 建议动作：`investigate`。
- 需要用户确认：是，涉及 systemd 启用策略，不自动修改。

### 7. shared-memory embeddings 数少于 memories 数

- 严重级别：低。
- 统计：memories=103，memory_embeddings=100。
- 判断：有 3 条记忆没有 embedding，可能是 disabled/特殊类型/写入时间差，也可能是索引缺口。
- 建议动作：`investigate`。
- 需要用户确认：否。

### 8. wiki_lint 显示来源证明不足

- 严重级别：中。
- 统计：9 个 provenance gaps，1 个 open question。
- 判断：没有发现明确矛盾，但来源不足会降低后续任务可验证性。
- 建议动作：`rewrite`，补证据或标记低置信度。
- 需要用户确认：否。

### 9. Obsidian 共享记忆导出存在多份摘要/报告入口

- 严重级别：低。
- 涉及路径：`/root/obsidian-vault/shared-memory/exports/`、`/root/obsidian-vault/shared-memory/reports/`、`/root/obsidian-vault/记忆治理/openclaw-session-memory-compact-*.md`。
- 判断：这些文件适合作为审计资料，但普通召回时容易与当前事实混杂。
- 建议动作：`merge`，建立“当前事实入口”和“历史导出入口”的区分。
- 需要用户确认：否。

### 10. 旧服务端口记忆可能与当前五位端口迁移混杂

- 严重级别：中。
- 涉及路径：`memory/archive/2026-06-25.md`、`memory/backups/openclaw-session-memory-compact-20260712T030000Z/2026-06-25.md`。
- 判断：同一报告中同时出现旧端口与后续五位端口迁移摘要。若只截取早段内容，可能误判当前服务端口。
- 建议动作：`rewrite`，将旧端口标成历史快照，当前端口另列。
- 需要用户确认：是。

## 建议动作汇总

| 动作 | 对象 | 说明 |
|------|------|------|
| keep | OpenClaw memory 向量索引、qwen 历史化说明、已 disabled 的历史低价值项 | 当前不应删除 |
| disable | 常规召回中的 `memory/backups/` 命中 | 仅建议检索层降权/排除，不删除文件 |
| expire | 旧 BJT 03:00-05:00 自动学习任务、历史 failed local_task | 防止当作当前规则 |
| merge | archive/backup 重复命中、Obsidian 导出入口 | 降低重复召回 |
| rewrite | local-first 全局前置路由、旧端口迁移记忆、provenance gaps | 明确历史/当前边界 |
| investigate | shared-agent-memory disabled 自启状态、3 条缺失 embedding | 需要确认是否为预期 |

## 最高优先级 3 项

1. 旧 BJT 03:00-05:00 自动学习任务：建议标为历史/过期，避免误导调度。
2. local-first/qwen 全局前置路由：旧决策已大多 disabled，但备份/归档仍会命中，建议统一改写为历史策略。
3. `memory/backups/` 被普通检索命中：建议从常规召回降权或排除，保留为审计备份。

## 待用户确认

- 是否将旧 BJT 03:00-05:00 自动学习任务正式标记为 `expire`。
- 是否将 local-first/qwen 全局前置路由相关摘要统一 `rewrite` 为历史项。
- 是否将 `memory/backups/` 从常规语义召回中降权或排除。
- shared-agent-memory 的 systemd `disabled` 是否为预期策略。
- 旧端口与五位端口迁移记忆是否需要整理成单独“当前服务端口”页面。

## 保存位置

`/root/obsidian-vault/记忆治理/2026-07-12-OpenClaw记忆质量巡检.md`
