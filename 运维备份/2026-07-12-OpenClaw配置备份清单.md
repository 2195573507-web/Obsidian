# OpenClaw 配置备份清单（2026-07-12）

> 范围：OpenClaw 配置与运行状态、Hermes 配置、Hermes gateway 相关补丁目录、shared-agent-memory、相关 systemd unit/timer。  
> 安全：仅记录路径、状态、大小、备份线索与回滚建议；不输出密钥/API key 原文；未重启服务，未修改主配置。

## 摘要
- 缺口数量：2
- 本次创建的小备份：7
  - `/root/.hermes/config.yaml.2026-07-12.bak`
  - `/etc/systemd/system/hermes-gateway.service.2026-07-12.bak`
  - `/etc/systemd/system/shared-agent-memory.service.2026-07-12.bak`
  - `/etc/systemd/system/shared-local-task-worker.service.2026-07-12.bak`
  - `/etc/systemd/system/qwen-warm.service.2026-07-12.bak`
  - `/etc/systemd/system/shared-memory-vector-index.service.2026-07-12.bak`
  - `/etc/systemd/system/shared-memory-vector-index.timer.2026-07-12.bak`
- 报告路径：`/root/obsidian-vault/运维备份/2026-07-12-OpenClaw配置备份清单.md`

## 检查结论
### 1. OpenClaw 配置与运行状态
- 当前运行态：本会话的 OpenClaw runtime 正常；`myproxy/gpt-5.5`。
- 参考元数据中可见：`~/.openclaw/openclaw.json`、`~/.openclaw/openclaw.json.bak`、若干历史快照与会话级备份。
- 近一周未发现需要补建的大体积备份。
- 回滚路径：`~/.openclaw/openclaw.json` 与既有 `.bak` / 会话级快照。

### 2. Hermes 配置 `/root/.hermes/config.yaml`
- 文件存在，大小 12536 bytes。
- 既有备份线索存在，且本次已补小备份：`/root/.hermes/config.yaml.2026-07-12.bak`。
- 回滚路径：同目录 `.bak` 与历史 `.bak.*` 文件。

### 3. Hermes gateway 补丁目录 `/opt/hermes-agent/gateway/`
- 目录存在，约 7.8M。
- 近 7 天未看到明显补丁/回滚文件或 Git 仓库线索。
- 目录内现有备份痕迹主要是旧的 `.bak` 文件：
  - `run.py.bak.openclaw-local-first-20260621060704`
  - `platforms/api_server.py.bak.openclaw-local-first-20260621060704`
  - `delivery.py.bak.openclaw-local-task-20260621084952`
- 回滚路径：目前偏弱，建议以后优先保留 patch/diff 或纳入 Git。

### 4. shared-agent-memory `/opt/shared-agent-memory/`
- 目录存在，约 587M。
- 近 7 天有活跃改动，尤其是 `data/memory.sqlite` 与 `vector-index/` 下的索引文件。
- 目录内可见旧备份：`main.py.bak.*`、`data/memory.sqlite.bak.*`。
- 回滚路径：能回到旧的代码/数据库备份，但当前对最近一周索引更新的可逆记录不够完整。

### 5. systemd units/timers
- `hermes-gateway.service`：active，已补 `.bak`。
- `shared-agent-memory.service`：active，已补 `.bak`。
- `shared-local-task-worker.service` / `.timer`：inactive / timer 存在，已补 `.bak`。
- `qwen-warm.service` / `.timer`：inactive / timer 存在，已补 `.bak`。
- `shared-memory-vector-index.service` / `.timer`：最近一次触发成功，已补 `.bak`。
- 回滚路径：同目录 `.bak` + `systemctl cat` 输出。

## 缺口
1. `/opt/hermes-agent/gateway/`：未发现近期补丁/变更清单或 Git 型回滚记录，源码补丁回滚路径不足。
2. `/opt/shared-agent-memory/`：近期改动较活跃，但缺少成体系的补丁/提交/回滚清单。

## 建议
- 对 `/opt/hermes-agent/gateway/` 和 `/opt/shared-agent-memory/` 建立轻量变更记录：优先 Git，其次 patch/diff + 简短说明。
- 继续保留同目录 `.bak`，但只针对小文件；避免复制大数据目录。
- 对 systemd unit/timer 修改后保留 `systemctl cat` 摘要和 `.bak`，便于快速回滚。
- `shared-agent-memory` 的索引与数据库属于高频变更区，建议单独维护回滚说明，不依赖整目录备份。

## 近期证据摘录
- `hermes-gateway.service`：`/etc/systemd/system/hermes-gateway.service`
- `shared-agent-memory.service`：`/etc/systemd/system/shared-agent-memory.service`
- `shared-local-task-worker.timer`：`/etc/systemd/system/shared-local-task-worker.timer`
- `qwen-warm.timer`：`/etc/systemd/system/qwen-warm.timer`
- `shared-memory-vector-index.timer`：`/etc/systemd/system/shared-memory-vector-index.timer`
