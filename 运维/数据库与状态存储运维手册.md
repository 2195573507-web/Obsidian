---
title: "数据库与状态存储运维手册"
type: wiki
category: "运维"
updated: 2026-07-05
managed_by: Hermes
tags: [ops, database, backup]
---

# 数据库与状态存储运维手册

> 本文是服务器知识库全量扩写的一部分，用于把零散经验沉淀为可复用知识。相关入口：[[server/服务器运维 Runbook|服务器运维 Runbook]]、[[运维/Docker 服务部署与容器运维|Docker 服务部署与容器运维]]。

## 适用场景

- 作为日常排障、设计评审、方案写作和 Agent 执行任务时的背景材料。
- 用于把一次性经验转成稳定 SOP，减少重复解释。
- 适合和服务器现有 [[server/服务器运维 Runbook|服务器运维 Runbook]]、[[agents/index|Agent 总览]]、[[shared-memory/README|共享记忆说明]] 联动使用。

## 核心原则

1. **先观测再行动**：所有配置、服务、数据库和知识结构修改前，先确认当前状态、备份点和回滚路径。
2. **最小可验证变更**：每次只改变一个可解释的变量，并记录命令、输出、时间和影响面。
3. **知识可追溯**：稳定事实写入 wiki，临时状态写入报告，危险动作保留人工确认。
4. **自动化但不自动删除**：可以批量生成索引、候选清单和质量报告，但重复文件、旧记忆、服务配置不自动删除。

## 操作清单

- 明确目标：这是排障、设计、扩写、迁移、审计还是复盘。
- 收集证据：文件、日志、服务状态、Git diff、健康接口、最近报告。
- 建立链接：新笔记至少链接一个 MOC 或上级主题，避免孤立知识。
- 验证结果：读回文件、检查 frontmatter、检查 wikilink、必要时访问 Web/health 接口。

## 常见风险

- 把日报或巡检报告当成永久事实，导致过期信息污染决策。
- 缺少 frontmatter、缺少链接，知识库变成不可导航的文件堆。
- 将服务器实时状态写死到概念笔记中，而不是链接到带时间戳的体检报告。
- 为了“整理”而删除历史资料；正确做法是先生成重复/过期候选报告。

## 数据库结构原则

服务器上的数据库/状态存储应分三类：业务数据、Agent 状态、索引缓存。业务数据需要备份、迁移脚本和 schema 记录；Agent 状态例如 Hermes `state.db`、cron jobs、sessions 需要谨慎读写；索引缓存例如知识库搜索索引可以重建，不应当作为唯一事实来源。

设计表结构时优先使用稳定主键、created_at/updated_at、source_path、checksum、status、error、metadata JSON 字段。对知识库来说，核心实体可以抽象为 Note、Link、Tag、Source、Run、Report、Memory。

## 备份与恢复

- 写操作前确认数据路径和服务使用者。
- SQLite 优先使用 `.backup` 或停止写入后复制，避免热复制损坏。
- 大规模迁移先生成 dry-run 报告，再执行。
- 恢复演练至少验证：文件存在、schema 可读、服务启动、查询结果正确。

## 关联笔记

- [[server/服务器运维 Runbook|服务器运维 Runbook]]
- [[运维/Docker 服务部署与容器运维|Docker 服务部署与容器运维]]
